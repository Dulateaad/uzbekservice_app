# Быстрая настройка iOS Firebase

## 🚀 Самый простой способ

### Шаг 1: Скачать файл

1. Откройте в браузере:
   ```
   https://console.firebase.google.com/project/odo-uz-1f4d9/settings/general
   ```

2. Прокрутите до раздела **"Your apps"**

3. Если iOS приложения нет:
   - Нажмите **"Add app"** → **iOS**
   - Bundle ID: `com.example.uzbekserviceApp`
   - Нажмите **"Register app"**

4. Нажмите **"Download GoogleService-Info.plist"**
   - Файл скачается в папку **Downloads**

### Шаг 2: Автоматическая установка

Выполните в терминале:
```bash
cd ~/uzbekservice_app
./setup_ios_firebase.sh
```

Скрипт автоматически:
- Найдет файл в Downloads
- Скопирует его в правильное место
- Покажет результат

### Шаг 3: Установить зависимости

```bash
cd ios
pod install
cd ..
```

### Шаг 4: Проверить

```bash
ls -la ios/Runner/GoogleService-Info.plist
```

Должен показать файл.

## 📝 Ручная установка (если скрипт не работает)

```bash
# Скопировать файл
cp ~/Downloads/GoogleService-Info.plist ~/uzbekservice_app/ios/Runner/

# Открыть Xcode и добавить файл в проект
open ios/Runner.xcworkspace
```

В Xcode:
1. Перетащите `GoogleService-Info.plist` в папку **Runner**
2. Отметьте **"Copy items if needed"**
3. Отметьте **"Add to targets: Runner"**
4. Нажмите **"Finish"**

## ✅ Готово!

После этого можно запускать приложение:
```bash
flutter run
```

