# Тестирование Firebase Authentication и Firestore

## ✅ Что уже настроено

1. ✅ Firebase Authentication (Phone) настроен в Firebase Console
2. ✅ Firestore Database создан
3. ✅ Код для работы с Authentication готов (`firebase_auth_service.dart`)
4. ✅ Код для работы с Firestore готов (`firebase_database_service.dart`)
5. ✅ Провайдеры настроены (`auth_provider.dart`, `firestore_auth_provider.dart`)

## 🧪 Как протестировать

### Шаг 1: Запустите приложение

```bash
cd ~/uzbekservice_app
flutter run
```

### Шаг 2: Проверьте инициализацию Firebase

В логах должно появиться:
```
✅ Firebase инициализирован успешно!
🔐 Готов к аутентификации пользователей
```

### Шаг 3: Тестирование Authentication

#### Вариант 1: Через тестовый номер (рекомендуется для разработки)

1. В Firebase Console → Authentication → Sign-in method → Phone
2. Добавьте тестовый номер в "Phone numbers for testing":
   - Phone number: `+998901234567`
   - Code: `123456`
3. В приложении используйте этот номер для входа
4. Введите код `123456` - вход должен пройти автоматически

#### Вариант 2: Реальный SMS (требует настройки)

1. Используйте реальный номер телефона
2. Firebase отправит SMS с кодом
3. Введите код в приложение

### Шаг 4: Проверка создания пользователя в Firestore

После успешной аутентификации:

1. Откройте Firebase Console → Firestore Database
2. Проверьте коллекцию `users` - должен появиться новый документ
3. Проверьте коллекцию `clients` или `specialists` (в зависимости от типа пользователя)

## 📋 Структура данных в Firestore

### Коллекция `users`
```json
{
  "id": "user_uid",
  "phoneNumber": "+998901234567",
  "name": "Имя пользователя",
  "email": "email@example.com",
  "userType": "client" | "specialist",
  "category": "категория",
  "description": "описание",
  "pricePerHour": 50000,
  "avatar": "url",
  "createdAt": "2024-11-29T...",
  "updatedAt": "2024-11-29T...",
  "isVerified": true
}
```

### Коллекция `specialists`
```json
{
  "userId": "user_uid",
  "phoneNumber": "+998901234567",
  "name": "Имя специалиста",
  "category": "категория",
  "rating": 0.0,
  "totalReviews": 0,
  "isActive": true,
  "createdAt": "timestamp",
  "updatedAt": "timestamp"
}
```

### Коллекция `clients`
```json
{
  "userId": "user_uid",
  "phoneNumber": "+998901234567",
  "name": "Имя клиента",
  "createdAt": "timestamp",
  "updatedAt": "timestamp"
}
```

## 🔧 Проверка работы через код

### Пример использования Authentication:

```dart
// Отправка SMS кода
final authService = FirebaseAuthService();
final result = await authService.sendSmsCode('+998901234567');
print('SMS отправлен: ${result['success']}');

// Проверка кода
final verified = await authService.verifySmsCode('+998901234567', '123456');
print('Верификация: $verified');
```

### Пример создания пользователя в Firestore:

```dart
// После успешной аутентификации
final dbService = FirebaseDatabaseService();
final user = await dbService.createUser(
  phoneNumber: '+998901234567',
  name: 'Тестовый пользователь',
  userType: 'client',
);
print('Пользователь создан: ${user.id}');
```

## ⚠️ Важные моменты

### Правила безопасности Firestore

Убедитесь, что правила настроены правильно. Для тестирования можно использовать:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

⚠️ **ВНИМАНИЕ**: Это правило разрешает всем авторизованным пользователям читать и писать. Для продакшена используйте более строгие правила (см. `FIREBASE_QUICK_SETUP.md`).

### Проблемы с SMS

Если SMS не приходит:
1. Проверьте, что Phone Authentication включен в Firebase Console
2. Убедитесь, что номер добавлен в тестовые номера (для разработки)
3. Проверьте квоты Firebase (бесплатный план имеет ограничения)
4. Для продакшена может потребоваться настройка reCAPTCHA

### Проверка подключения

```dart
// Проверка текущего пользователя
final currentUser = FirebaseConfig.currentUser;
print('Текущий пользователь: ${currentUser?.uid}');

// Проверка подключения к Firestore
try {
  await FirebaseFirestore.instance
    .collection('test')
    .doc('connection')
    .set({'test': true});
  print('✅ Firestore подключен');
} catch (e) {
  print('❌ Ошибка Firestore: $e');
}
```

## 🐛 Решение проблем

### Проблема: "FirebaseApp not initialized"
**Решение**: Убедитесь, что `FirebaseConfig.initialize()` вызывается в `main()` до `runApp()`

### Проблема: "Permission denied" в Firestore
**Решение**: Проверьте правила безопасности в Firebase Console → Firestore → Rules

### Проблема: SMS код не приходит
**Решение**: 
- Используйте тестовый номер для разработки
- Проверьте настройки Phone Authentication
- Проверьте квоты Firebase

### Проблема: Пользователь не создается в Firestore
**Решение**: 
- Убедитесь, что пользователь аутентифицирован (`FirebaseConfig.currentUser != null`)
- Проверьте правила Firestore
- Проверьте логи на наличие ошибок

## 📝 Следующие шаги

1. ✅ Настроить правила безопасности Firestore для продакшена
2. ✅ Настроить правила Storage
3. ✅ Добавить обработку ошибок
4. ✅ Настроить push-уведомления (Firebase Cloud Messaging)
5. ✅ Добавить аналитику (Firebase Analytics)

## 🔗 Полезные ссылки

- [Firebase Console](https://console.firebase.google.com/project/odo-uz-1f4d9)
- [Firestore Database](https://console.firebase.google.com/project/odo-uz-1f4d9/firestore)
- [Authentication](https://console.firebase.google.com/project/odo-uz-1f4d9/authentication)

