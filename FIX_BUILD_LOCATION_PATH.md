# 🔧 Исправление неправильного пути Build Location

## ❌ Проблема:

В ошибке виден неправильный путь:
```
/Users/dulatea/uzbekservice_app/ios/   /Users/dulatea/XcodeDerivedData/Build/Products/...
```

Это означает, что в настройках **Build Location** указан неправильный путь.

## ✅ Решение:

### Шаг 1: Откройте настройки Build Location

1. В Xcode: **Xcode → Settings** (`Cmd + ,`)
2. Перейдите на вкладку **"Locations"**
3. В разделе **"Derived Data"** нажмите **"Build Location..."**

### Шаг 2: Исправьте настройки Build Location

В диалоге **"Build Location"**:

1. Выберите **"Relative to Derived Data"** (если еще не выбрано)
2. В поле **"Products"** должно быть:
   ```
   Build/Products
   ```
   **НЕ должно быть:**
   - `/Users/dulatea/XcodeDerivedData` ❌
   - Полный путь ❌
   - Два пути склеенных вместе ❌

3. В поле **"Intermediates"** должно быть:
   ```
   Build/Intermediates.noindex
   ```

4. Нажмите **"Done"**

### Шаг 3: Альтернатива - используйте "Unique" или "Shared Folder"

Если "Custom" не работает, попробуйте:

1. В диалоге **"Build Location"** выберите **"Unique"**
2. Нажмите **"Done"**

Или:

1. Выберите **"Shared Folder"**
2. Нажмите **"Done"**

### Шаг 4: Закройте и перезапустите Xcode

1. Закройте окно **"Locations"**
2. **Полностью закройте Xcode** (`Cmd + Q`)
3. Откройте проект заново:
   ```bash
   open ~/uzbekservice_app/ios/Runner.xcworkspace
   ```

### Шаг 5: Очистите и пересоберите

1. **Product → Clean Build Folder** (`Shift + Cmd + K`)
2. **Product → Build** (`Cmd + B`)

## ⚠️ Важно:

- **Products** и **Intermediates** должны быть **относительными путями**, если выбрано "Relative to Derived Data"
- Если выбран "Custom" с абсолютными путями, оба пути должны быть полными и правильными
- **НЕ должно быть** двух путей, склеенных вместе

## 💡 Рекомендация:

Лучше всего использовать **"Unique"** или **"Shared Folder"** вместо "Custom" - это избежит проблем с путями.

