# ⚡ Оптимизация приложения

## 📦 Уменьшение размера APK/AAB

### 1. Включить ProGuard/R8 (уже настроено)
- ✅ `android/app/build.gradle.kts` - ProGuard включен для release
- ✅ `android/app/proguard-rules.pro` - правила настроены

### 2. Оптимизация изображений
- Используйте WebP вместо PNG/JPG где возможно
- Сжимайте изображения перед добавлением в assets
- Используйте `cached_network_image` для сетевых изображений (уже используется)

### 3. Удаление неиспользуемых зависимостей
Проверьте `pubspec.yaml` и удалите неиспользуемые пакеты.

### 4. Tree Shaking
Flutter автоматически удаляет неиспользуемый код при сборке release.

## 🚀 Улучшение производительности

### 1. Ленивая загрузка
- Используйте `ListView.builder` вместо `ListView` для длинных списков
- Загружайте изображения по требованию

### 2. Кэширование
- Используйте `cached_network_image` для изображений (уже используется)
- Кэшируйте данные из Firestore локально

### 3. Оптимизация анимаций
- Используйте `RepaintBoundary` для сложных виджетов
- Ограничьте количество одновременных анимаций

### 4. Оптимизация Firestore запросов
- Используйте индексы для частых запросов
- Ограничивайте количество документов через `limit()`
- Используйте `where()` с индексами

## 📊 Проверка размера

### Android
```bash
flutter build appbundle --release
# Размер будет в build/app/outputs/bundle/release/app-release.aab
```

### iOS
```bash
flutter build ios --release
# Размер будет в build/ios/iphoneos/Runner.app
```

## 🔍 Анализ размера

### Android
```bash
# Анализ APK
flutter build apk --release --analyze-size
```

### iOS
Используйте Xcode → Product → Archive → Distribute App → Analyze

## ✅ Рекомендации

1. **Минимальный размер**: Старайтесь держать APK < 50MB, AAB < 100MB
2. **Производительность**: Время запуска < 3 секунд
3. **Память**: Использование RAM < 200MB в среднем
4. **Батарея**: Минимизируйте фоновые операции

