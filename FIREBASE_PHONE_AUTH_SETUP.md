# 📱 Настройка Firebase Phone Authentication для реальной отправки SMS

## ✅ Что сделано:

1. **Обновлен `phone_auth_screen.dart`:**
   - Использует `FirebaseAuthService` вместо `SimpleSmsService`
   - Реальная отправка SMS через Firebase Phone Authentication
   - Правильное форматирование номеров телефонов

2. **Обновлен `firestore_auth_provider.dart`:**
   - Использует `FirebaseAuthService` для отправки и проверки SMS
   - Интеграция с Firebase Auth для создания пользователей

3. **Обновлен `sms_verification_screen.dart`:**
   - Поддержка `verificationId` от Firebase
   - Правильная передача данных между экранами

## 🔧 Настройка Firebase Console:

### 1. Включить Phone Authentication

1. Перейдите в [Firebase Console](https://console.firebase.google.com/)
2. Выберите проект: **odo-uz-1f4d9**
3. Перейдите: **Authentication** → **Sign-in method**
4. Найдите **Phone** и нажмите **Enable**
5. Сохраните настройки

### 2. Настроить тестовые номера (для разработки)

В Firebase Console → Authentication → Sign-in method → Phone:

1. Нажмите на **Phone** для редактирования
2. В разделе **Phone numbers for testing** добавьте тестовые номера:
   - Формат: `+998901234567`
   - Код: `123456` (или любой другой)
3. Это позволит тестировать без реальной отправки SMS

### 3. Настроить квоты (для продакшена)

1. Перейдите: **Firebase Console** → **Usage and billing**
2. Проверьте лимиты на Phone Authentication
3. По умолчанию: **10 SMS в день** (бесплатный план)
4. Для увеличения лимита нужно настроить оплату

## 📱 Формат номеров телефонов:

Приложение автоматически форматирует номера:
- **Узбекистан:** `+998901234567`
- **Казахстан:** `+77771234567`

## 🔍 Проверка работы:

### Тестирование:

1. **С тестовым номером:**
   - Добавьте номер в Firebase Console как тестовый
   - Используйте указанный код для входа

2. **С реальным номером:**
   - Введите реальный номер телефона
   - SMS придет автоматически
   - Введите полученный код

### Логи для отладки:

```dart
print('📱 Отправка SMS на номер: $phoneNumber');
print('✅ SMS код отправлен через Firebase');
print('🔐 Проверка кода...');
```

## ⚠️ Важные моменты:

1. **Лимиты SMS:**
   - Бесплатный план: 10 SMS/день
   - Платный план: зависит от тарифа

2. **Стоимость:**
   - Firebase Phone Auth использует Google Cloud
   - Стоимость зависит от страны и оператора
   - Обычно: $0.01-0.05 за SMS

3. **Поддерживаемые страны:**
   - Узбекистан: ✅
   - Казахстан: ✅
   - Другие страны: проверьте в Firebase Console

## 🚀 После настройки:

1. Включите Phone Authentication в Firebase Console
2. Протестируйте с тестовым номером
3. Протестируйте с реальным номером
4. Настройте квоты для продакшена

## 📝 Код для проверки:

```dart
// Отправка SMS
final result = await FirebaseAuthService().sendSmsCode('+998901234567');
if (result['success']) {
  print('SMS отправлен!');
}

// Проверка кода
final isValid = await FirebaseAuthService().verifySmsCode(
  '+998901234567',
  '123456',
);
```

## ✅ Готово!

После настройки Firebase Console SMS будут отправляться реально!

