# Настройка тестового режима - экран всегда включен

## Что сделано

Добавлена поддержка автоматического включения режима "экран всегда включен" в тестовом режиме (debug).

### Изменения:

1. **Добавлен пакет `wakelock_plus`** в `pubspec.yaml`
2. **Обновлен `lib/main.dart`** - автоматически включает wakelock в debug режиме

## Как это работает

В режиме отладки (когда запускаете через `flutter run` или в IDE с debug режимом) экран устройства будет оставаться включенным и не будет блокироваться.

### В коде:

```dart
// В тестовом режиме (debug) держим экран включенным
if (kDebugMode) {
  await WakelockPlus.enable();
  print('🔋 Режим тестирования: экран будет оставаться включенным');
}
```

## Использование

### Автоматически (рекомендуется)

Просто запустите приложение в debug режиме:
```bash
flutter run
```

Экран автоматически останется включенным.

### Вручную (для production или других случаев)

Если нужно включить/выключить вручную в коде:

```dart
import 'package:wakelock_plus/wakelock_plus';

// Включить
await WakelockPlus.enable();

// Выключить
await WakelockPlus.disable();

// Проверить статус
bool isEnabled = await WakelockPlus.enabled;
```

## Платформы

- ✅ **Android** - работает автоматически
- ✅ **iOS** - работает автоматически  
- ✅ **Web** - работает автоматически
- ✅ **macOS** - работает автоматически
- ✅ **Windows** - работает автоматически
- ✅ **Linux** - работает автоматически

## Важно

- ⚠️ В **release режиме** (production) экран будет работать нормально (будет блокироваться)
- ✅ В **debug режиме** экран остается включенным для удобства тестирования
- 🔋 Это может разряжать батарею быстрее, поэтому используется только в тестовом режиме

## Отключение

Если нужно отключить эту функцию, просто закомментируйте или удалите код в `main.dart`:

```dart
// Закомментировать эти строки:
// if (kDebugMode) {
//   await WakelockPlus.enable();
//   print('🔋 Режим тестирования: экран будет оставаться включенным');
// }
```

## Проверка

После запуска приложения в debug режиме вы должны увидеть в консоли:
```
🔋 Режим тестирования: экран будет оставаться включенным
```

И экран устройства не будет блокироваться автоматически.

