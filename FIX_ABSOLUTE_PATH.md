# ✅ Исправление ошибки "must be an absolute path"

## ❌ Ошибка:
"Custom derived data location must be an absolute path. Please provide a valid value."

## ✅ Решение:

### Шаг 1: Нажмите "OK" в диалоге ошибки

Закройте диалог с ошибкой, нажав **"OK"**.

### Шаг 2: Проверьте путь в настройках

В окне **"Locations"** проверьте раздел **"Derived Data"**.

Должно быть:
```
Derived Data
/Users/dulatea/XcodeDerivedData
[Custom] [Build Location...]
```

### Шаг 3: Если путь неправильный, исправьте его

1. **Кликните на путь** `/Users/dulatea/XcodeDerivedData`
2. Или нажмите на **"Custom"** рядом с путем
3. Убедитесь, что путь указан **полностью**, начиная с `/`:
   ```
   /Users/dulatea/XcodeDerivedData
   ```
   
   **НЕ используйте:**
   - `~/XcodeDerivedData` ❌
   - `XcodeDerivedData` ❌
   - `Users/dulatea/XcodeDerivedData` ❌ (без начального `/`)

4. Нажмите **"Done"**

### Шаг 4: Проверьте, что директория существует

Убедитесь, что директория создана:
```bash
ls -ld ~/XcodeDerivedData
```

Если директории нет, создайте её:
```bash
mkdir -p ~/XcodeDerivedData
```

### Шаг 5: Закройте и перезапустите Xcode

1. Закройте окно **"Locations"** (нажмите "Done")
2. **Полностью закройте Xcode** (`Cmd + Q`)
3. Откройте проект заново:
   ```bash
   open ~/uzbekservice_app/ios/Runner.xcworkspace
   ```
4. **Product → Clean Build Folder** (`Shift + Cmd + K`)
5. **Product → Build** (`Cmd + B`)

## 🔍 Проверка:

После перезапуска Xcode, в настройках **Locations → Derived Data** должно быть:
```
/Users/dulatea/XcodeDerivedData
[Custom]
```

И **НЕ должно быть** ошибки "must be an absolute path".

## 💡 Если ошибка все еще есть:

Попробуйте:
1. Полностью удалить настройку (вернуть на "Default")
2. Закрыть Xcode
3. Открыть Xcode заново
4. Снова установить Custom путь: `/Users/dulatea/XcodeDerivedData`

