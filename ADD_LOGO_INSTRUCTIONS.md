# 🎨 Инструкция по добавлению логотипа ODO

## 📁 Куда сохранить:

Сохраните логотип в файл:
```
assets/images/logo.jpg
```

Или в формате PNG:
```
assets/images/logo.png
```

## 📋 Шаги:

### 1. Сохраните изображение

**Вариант A: Через Finder (macOS)**
1. Откройте Finder
2. Перейдите в: `~/uzbekservice_app/assets/images/`
3. Перетащите файл логотипа туда
4. Переименуйте в `logo.jpg` или `logo.png`

**Вариант B: Через терминал**
```bash
cd ~/uzbekservice_app/assets/images/
# Скопируйте ваш файл логотипа сюда и переименуйте в logo.jpg
```

### 2. Обновите код (если нужно)

Если вы сохранили как PNG, обновите путь в `lib/widgets/odo_logo.dart`:
```dart
'assets/images/logo.png',  // вместо logo.jpg
```

### 3. Перезапустите приложение

```bash
flutter run -d chrome
```

## ✅ Текущая настройка:

- **Путь в коде:** `assets/images/logo.jpg`
- **Fallback:** Если файл не найден, показывается текстовый логотип "ODO.UZ"
- **Размеры:** Автоматически подстраивается под указанные width/height

## 🎨 Рекомендации для логотипа:

- **Формат:** JPG или PNG
- **Размер:** Рекомендуется 512x512 px или больше (для разных разрешений)
- **Фон:** Прозрачный (PNG) или белый/черный
- **Качество:** Высокое (для четкости на всех устройствах)

## 📝 После добавления:

Логотип будет автоматически использоваться в:
- Splash screen
- Onboarding screen
- Login screen
- И других местах, где используется `OdoLogo` виджет

---

**После сохранения файла перезапустите приложение!**

