#!/bin/bash
echo "🌐 Запуск приложения с доступом по сети..."
IP=$(ifconfig | grep 'inet ' | grep -v 127.0.0.1 | head -1 | awk '{print $2}')
echo "📱 Ваш IP: $IP"
echo "🔗 URL для других устройств: http://$IP:8112"
echo ""
echo "💡 Убедитесь, что другие устройства подключены к той же Wi-Fi сети"
echo ""
flutter run -d chrome --web-hostname 0.0.0.0 --web-port=8112
