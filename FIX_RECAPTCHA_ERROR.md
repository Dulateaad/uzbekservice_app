# 🔧 Исправление ошибки reCAPTCHA "TOO_LONG"

## ❌ Проблема:
```
Failed to initialize reCAPTCHA Enterprise config. Triggering the reCAPTCHA v2 verification.
❌ Ошибка верификации: TOO_LONG
```

## 🔍 Причины:
1. **Номер телефона слишком длинный** - неправильное форматирование
2. **reCAPTCHA не настроен** для веб-платформы в Firebase
3. **Неправильный формат номера** - лишние символы

## ✅ Решения:

### 1. Исправлено форматирование номера
- Убраны пробелы, дефисы, скобки
- Проверка длины номера (макс 16 символов)
- Правильное добавление кода страны

### 2. Настроить reCAPTCHA для веб (если ошибка продолжается)

#### В Firebase Console:
1. Перейдите: **Authentication** → **Settings** → **Authorized domains**
2. Убедитесь, что ваш домен добавлен:
   - `localhost` (для разработки)
   - `odo-uz-1f4d9.web.app` (для продакшена)
   - `odo-uz-1f4d9.firebaseapp.com` (для продакшена)

#### Настроить reCAPTCHA:
1. Перейдите: **Authentication** → **Settings** → **reCAPTCHA Enterprise**
2. Если не настроено, Firebase автоматически использует reCAPTCHA v2
3. Для продакшена рекомендуется настроить reCAPTCHA Enterprise

### 3. Альтернатива: Использовать тестовые номера
В Firebase Console → Authentication → Sign-in method → Phone:
- Добавьте тестовые номера
- Используйте указанные коды
- reCAPTCHA не требуется для тестовых номеров

## 🧪 Проверка:

После исправлений:
1. Перезапустите приложение
2. Введите номер в правильном формате: `+998901234567`
3. Проверьте, что ошибка исчезла

## 📝 Формат номеров:

**Правильно:**
- `+998901234567` ✅
- `998901234567` ✅ (автоматически добавится +)
- `901234567` ✅ (автоматически станет +998901234567)

**Неправильно:**
- `+998 90 123 45 67` ❌ (пробелы будут убраны)
- `998 90 123 45 67` ❌ (пробелы будут убраны)
- `+998901234567890` ❌ (слишком длинный)

## ⚠️ Если ошибка продолжается:

1. Проверьте, что Phone Auth включен в Firebase Console
2. Проверьте формат номера (должен быть E.164: +код_страны номер)
3. Используйте тестовые номера для разработки
4. Проверьте лимиты SMS в Firebase Console

