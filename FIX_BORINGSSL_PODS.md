# 🔧 Исправление BoringSSL-GRPC ошибок

## ❌ Проблема:
- **Minimum Deployments** уже **iOS 13.0** ✅
- Но ошибки BoringSSL-GRPC все еще есть: "unsupported option '-G' for target 'arm64-apple-ios12.0'"
- Это происходит потому что **CocoaPods зависимости** все еще используют iOS 12.0

## ✅ Решение: Переустановить CocoaPods зависимости

### Шаг 1: Проверьте CocoaPods

```bash
which pod
```

Если команда не найдена, установите CocoaPods:
```bash
sudo gem install cocoapods
```

### Шаг 2: Переустановите зависимости

```bash
cd ~/uzbekservice_app/ios
pod deintegrate
pod install
cd ..
```

### Шаг 3: Очистите проект в Xcode

1. **Product** → **Clean Build Folder** (`Shift + Cmd + K`)

### Шаг 4: Попробуйте сборку

1. **Product** → **Build** (`Cmd + B`)

## 🔍 Что происходит:

- `pod deintegrate` - удаляет все установленные pods
- `pod install` - переустанавливает pods с новыми настройками из Podfile
- В Podfile уже установлено `IPHONEOS_DEPLOYMENT_TARGET = '13.0'`
- После переустановки все pods будут использовать iOS 13.0

## ⚠️ Если CocoaPods не установлен:

### Установка CocoaPods:

```bash
sudo gem install cocoapods
```

После установки:
```bash
cd ~/uzbekservice_app/ios
pod install
cd ..
```

## 📋 Альтернативное решение (если не работает):

Если переустановка pods не помогает, попробуйте:

1. **Удалите папку Pods:**
```bash
cd ~/uzbekservice_app/ios
rm -rf Pods Podfile.lock
```

2. **Переустановите:**
```bash
pod install
```

3. **Очистите проект:**
```bash
cd ..
flutter clean
```

4. **В Xcode:**
   - **Product** → **Clean Build Folder**
   - **Product** → **Build**

## ✅ После исправления:

- Ошибки BoringSSL-GRPC должны исчезнуть
- Статус должен измениться на "Build Succeeded"
- Можно создавать Archive

## 📱 После успешной сборки:

1. **Product** → **Archive**
2. **Organizer** → **Distribute App**
3. Загрузка в TestFlight

