# 🚀 Сборка IPA через Flutter CLI (ОБХОДИТ Sandbox)

## ✅ Рекомендуемое решение:

Используйте `flutter build ipa` - это часто обходит все Sandbox проблемы!

## 📋 Выполните в терминале:

```bash
cd ~/uzbekservice_app
flutter build ipa --release
```

**Это создаст готовый IPA файл для загрузки в App Store Connect!**

## ⏱️ Время выполнения:

- Первая сборка: 10-20 минут
- Последующие сборки: 5-10 минут

## 📦 После сборки:

IPA файл будет создан в:
```
~/uzbekservice_app/build/ios/ipa/
```

## 📤 Загрузка в App Store Connect:

### Вариант 1: Через Transporter (рекомендуется)

1. Откройте **Transporter** app (из App Store)
2. Перетащите IPA файл в Transporter
3. Нажмите **Deliver**
4. IPA будет загружен в App Store Connect

### Вариант 2: Через Xcode Organizer

1. Откройте Xcode
2. **Window → Organizer**
3. Перетащите IPA файл в Organizer
4. Нажмите **Distribute App**

## ⚠️ Если сборка не удается:

### Проверьте настройки подписи:

1. Откройте `ios/Runner.xcodeproj` в Xcode
2. Выберите target **Runner**
3. Перейдите на вкладку **Signing & Capabilities**
4. Убедитесь, что выбрана правильная команда и профиль

### Или используйте без подписи (для тестирования):

```bash
flutter build ipa --release --export-method ad-hoc
```

## 💡 Преимущества этого метода:

- ✅ Обходит Xcode Sandbox проблемы
- ✅ Создает готовый IPA файл
- ✅ Можно загрузить в App Store Connect
- ✅ Не требует Archive в Xcode

## 🔍 Проверка результата:

После сборки проверьте:

```bash
ls -lh ~/uzbekservice_app/build/ios/ipa/
```

Должен быть файл `.ipa` готовый для загрузки.

