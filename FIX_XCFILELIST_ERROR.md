# 🔧 Исправление ошибки xcfilelist

## ❌ Ошибка:
```
Unable to load contents of file list: '/Target Support Files/Pods-Runner/Pods-Runner-resources-Debug-input-files.xcfilelist'
```

## ✅ Причина:
Отсутствуют файлы, созданные CocoaPods. Это происходит когда:
- Pods не были полностью установлены
- Файлы были удалены или повреждены
- Xcode открыт во время установки pods

## 🔧 Решение:

### Шаг 1: Переустановите CocoaPods зависимости

```bash
cd ~/uzbekservice_app/ios
pod install
```

Если команда `pod` не найдена, попробуйте полный путь:
```bash
/opt/homebrew/bin/pod install
# или
/usr/local/bin/pod install
```

### Шаг 2: Закройте и откройте Xcode

1. **Закройте Xcode полностью** (Cmd + Q)
2. **Откройте снова:**
   ```bash
   open ~/uzbekservice_app/ios/Runner.xcworkspace
   ```

### Шаг 3: Очистите проект

1. **Product** → **Clean Build Folder** (`Shift + Cmd + K`)

### Шаг 4: Попробуйте сборку

1. **Product** → **Build** (`Cmd + B`)

## 🔍 Если проблема сохраняется:

### Вариант 1: Полная переустановка

```bash
cd ~/uzbekservice_app/ios
rm -rf Pods Podfile.lock .symlinks
pod install
cd ..
flutter clean
flutter pub get
```

### Вариант 2: Обновите CocoaPods репозиторий

```bash
pod repo update
cd ~/uzbekservice_app/ios
pod install
```

### Вариант 3: Удалите Derived Data

1. В Xcode: **Preferences** → **Locations**
2. Нажмите на стрелку рядом с **Derived Data**
3. Удалите папку для вашего проекта
4. Перезапустите Xcode

## ✅ После исправления:

- ✅ Файлы xcfilelist должны быть созданы
- ✅ Ошибка должна исчезнуть
- ✅ Сборка должна пройти успешно

## 📱 После успешной сборки:

1. **Product** → **Archive**
2. **Organizer** → **Distribute App**
3. Загрузка в TestFlight

