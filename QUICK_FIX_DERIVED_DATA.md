# ⚡ Быстрое исправление: Изменение Derived Data пути

## ✅ Вы в правильном месте!

В окне **Locations** вы видите раздел **"Derived Data"** с путем:
```
/Users/dulatea/Library/Developer/Xcode/DerivedData
```

## 📋 Что сделать:

### Вариант 1: Изменить через кнопку "Build Location..."

1. **Нажмите на кнопку "Build Location..."** рядом с "Derived Data"
2. В открывшемся окне выберите **"Custom"**
3. Введите новый путь:
   ```
   /Users/dulatea/XcodeDerivedData
   ```
4. Нажмите **"Done"**

### Вариант 2: Изменить путь напрямую

1. **Кликните на путь** `/Users/dulatea/Library/Developer/Xcode/DerivedData`
2. Или нажмите на **"Default"** рядом с путем
3. Выберите **"Custom"** или введите:
   ```
   /Users/dulatea/XcodeDerivedData
   ```
4. Нажмите **"Done"**

## 🔄 После изменения:

1. **Закройте окно настроек** (нажмите "Done" или закройте окно)
2. **Полностью закройте Xcode** (`Cmd + Q`)
3. **Откройте проект заново:**
   ```bash
   open ~/uzbekservice_app/ios/Runner.xcworkspace
   ```
4. **Очистите Build:**
   - Product → Clean Build Folder (`Shift + Cmd + K`)
5. **Попробуйте сборку:**
   - Product → Build (`Cmd + B`)

## ✅ Проверка:

После перезапуска Xcode, путь должен измениться на:
```
/Users/dulatea/XcodeDerivedData
```

Это решит Sandbox проблемы!

