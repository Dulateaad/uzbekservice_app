# Быстрый старт с Supabase

## ✅ Что уже сделано:

1. ✅ Добавлен `supabase_flutter` в зависимости
2. ✅ Созданы сервисы Supabase:
   - `SupabaseAuthService` - аутентификация
   - `SupabaseDatabaseService` - работа с базой данных
   - `SupabaseStorageService` - хранение файлов
3. ✅ Обновлен `main.dart` для инициализации Supabase
4. ✅ Создана SQL схема (`supabase_schema.sql`)

## 📋 Следующие шаги:

### 1. Создайте проект Supabase

1. Перейдите на https://supabase.com
2. Зарегистрируйтесь/войдите
3. Создайте новый проект
4. Дождитесь завершения создания (1-2 минуты)

### 2. Настройте конфигурацию

1. В Supabase Dashboard перейдите в **Settings > API**
2. Скопируйте:
   - **Project URL** (например: `https://xxxxx.supabase.co`)
   - **anon public** key
3. Откройте `lib/config/supabase_config.dart`
4. Замените значения:
   ```dart
   static const String supabaseUrl = 'https://ваш-проект.supabase.co';
   static const String supabaseAnonKey = 'ваш-anon-key';
   ```

### 3. Создайте базу данных

1. В Supabase Dashboard откройте **SQL Editor**
2. Скопируйте содержимое файла `supabase_schema.sql`
3. Вставьте в SQL Editor и выполните (Run)
4. Проверьте созданные таблицы в **Table Editor**

### 4. Настройте Storage

1. Перейдите в **Storage** в Supabase Dashboard
2. Создайте bucket `images`:
   - Нажмите "New bucket"
   - Имя: `images`
   - Public: ✅ (включено)
3. Создайте bucket `files` (опционально):
   - Имя: `files`
   - Public: по необходимости

### 5. Настройте аутентификацию (SMS)

**Вариант A: Через Edge Functions (рекомендуется)**

1. Создайте Edge Function для отправки SMS:
   - Перейдите в **Edge Functions**
   - Создайте функцию `send-sms-code`
   - Используйте Twilio, SMS.ru или другой SMS провайдер

**Вариант B: Использовать внешний сервис**

Используйте существующий SMS сервис и интегрируйте его с Supabase Auth через OTP.

### 6. Обновите провайдеры

Нужно обновить провайдеры для использования Supabase сервисов:

**Пример:**
```dart
// Было:
import '../services/firebase_auth_service.dart';
final authService = FirebaseAuthService();

// Стало:
import '../services/supabase_auth_service.dart';
final authService = SupabaseAuthService();
```

### 7. Обновите экраны

Замените использование Firebase сервисов на Supabase в экранах:
- `lib/screens/auth/*` - экраны аутентификации
- `lib/providers/auth_provider.dart` - провайдер аутентификации
- `lib/providers/firestore_providers.dart` - провайдеры данных
- И другие файлы, использующие Firebase

## 🔄 Миграция данных (если нужно)

Если у вас уже есть данные в Firebase:

1. Экспортируйте данные из Firestore
2. Преобразуйте формат под PostgreSQL
3. Импортируйте в Supabase через SQL или API

## 📚 Полезные ссылки

- [Supabase Flutter Docs](https://supabase.com/docs/reference/dart/introduction)
- [Supabase Auth](https://supabase.com/docs/guides/auth)
- [Supabase Storage](https://supabase.com/docs/guides/storage)
- [Supabase Realtime](https://supabase.com/docs/guides/realtime)

## ⚠️ Важные замечания

1. **SMS аутентификация**: Supabase не имеет встроенной SMS. Нужно использовать Edge Functions или внешний сервис.

2. **Push уведомления**: Supabase не имеет встроенных push уведомлений. Можно использовать:
   - Supabase Realtime для уведомлений в приложении
   - Внешний сервис (OneSignal, Pusher)

3. **Тестирование**: После настройки протестируйте все функции перед удалением Firebase кода.

## 🚀 Готово к использованию

После выполнения всех шагов приложение будет работать с Supabase!

