# 🔧 Исправление через defaults (если Xcode не сохраняет настройку)

## ❌ Проблема:

Xcode не сохраняет настройку "Custom" для DerivedData и возвращается к "Relative" после перезапуска.

## ✅ Решение через командную строку:

### Шаг 1: Установите настройку через defaults

```bash
defaults write com.apple.dt.Xcode IDECustomDerivedDataLocation "/Users/dulatea/XcodeDerivedData"
```

### Шаг 2: Проверьте настройку

```bash
defaults read com.apple.dt.Xcode IDECustomDerivedDataLocation
```

Должно вывести: `/Users/dulatea/XcodeDerivedData`

### Шаг 3: Перезапустите Xcode

1. **Полностью закройте Xcode** (`Cmd + Q`)
2. Откройте проект заново:
   ```bash
   open ~/uzbekservice_app/ios/Runner.xcworkspace
   ```

### Шаг 4: Проверьте в настройках Xcode

1. **Xcode → Settings** (`Cmd + ,`)
2. Перейдите на вкладку **"Locations"**
3. Проверьте, что теперь:
   ```
   Derived Data: Custom
   /Users/dulatea/XcodeDerivedData
   ```

## 🔄 Альтернативное решение:

Если defaults не работает, попробуйте:

### Вариант 1: Использовать "Unique" вместо "Custom"

1. В настройках Locations → Derived Data
2. Выберите **"Default"** (не Custom)
3. Затем нажмите **"Build Location..."**
4. Выберите **"Unique"**
5. Нажмите "Done"

Это должно работать стабильнее.

### Вариант 2: Перезапустить Mac

Иногда помогает полный перезапуск системы для применения настроек.

## 💡 Почему это происходит:

Некоторые версии Xcode (особенно beta) могут иметь проблемы с сохранением настроек Custom paths. Использование `defaults` команды напрямую записывает настройку в системные preferences.

