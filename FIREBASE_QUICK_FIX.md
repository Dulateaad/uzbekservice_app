# 🔥 Быстрое исправление Firebase Firestore

## ❌ Проблема
```
[cloud_firestore/permission-denied] Missing or insufficient permissions.
```

## ✅ Решение (2 минуты)

### 1. Откройте Firebase Console
- Перейдите на: https://console.firebase.google.com
- Выберите проект: **odo-uz-app**

### 2. Настройте Firestore Rules
1. В левом меню выберите **"Firestore Database"**
2. Перейдите на вкладку **"Rules"**
3. Замените существующие правила на:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Временные правила для разработки (ОТКРЫТЫЙ ДОСТУП)
    // ⚠️ НЕ ИСПОЛЬЗУЙТЕ В ПРОДАКШЕНЕ!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

4. Нажмите **"Publish"**

### 3. Проверьте результат
После настройки правил:
- ✅ Регистрация пользователей будет работать
- ✅ Сохранение данных в Firestore будет работать
- ✅ Аутентификация будет работать корректно

## 🔒 Безопасность
Эти правила дают полный доступ к базе данных. Для продакшена нужно будет настроить более строгие правила с проверкой аутентификации.

## 📱 Тестирование
После настройки правил:
1. Перезапустите приложение
2. Попробуйте зарегистрироваться
3. Проверьте, что данные сохраняются в Firestore
