# 🔑 Обновление key.properties

## ✅ Keystore создан!

Файл `android/odo_uz_keystore.jks` успешно создан.

## 📝 Следующий шаг: Обновить key.properties

Откройте файл `android/key.properties` и замените плейсхолдеры на **реальные пароли**, которые вы вводили при создании keystore:

```properties
storePassword=ВАШ_ПАРОЛЬ_КЕЙСТОРА
keyPassword=ВАШ_ПАРОЛЬ_КЛЮЧА
keyAlias=odo_uz_key
storeFile=../odo_uz_keystore.jks
```

### Пример:
```properties
storePassword=MySecurePassword123!
keyPassword=MyKeyPassword456!
keyAlias=odo_uz_key
storeFile=../odo_uz_keystore.jks
```

## ⚠️ ВАЖНО: Безопасность

- ✅ Файл `android/key.properties` уже добавлен в `.gitignore`
- ✅ Файл `android/odo_uz_keystore.jks` уже добавлен в `.gitignore`
- ❌ **НИКОГДА** не коммитьте эти файлы в Git!
- 💾 Сохраните пароли в безопасном месте (менеджер паролей)

## 🚀 После обновления key.properties

Когда вы обновите `key.properties` с реальными паролями, можно будет собрать AAB:

```bash
./build_release.sh
```

## 📋 Проверка готовности

Перед сборкой убедитесь:
- [x] Keystore создан (`android/odo_uz_keystore.jks`)
- [ ] `key.properties` обновлен с реальными паролями
- [ ] Android SDK установлен (для сборки AAB)
- [ ] Версия в `pubspec.yaml` обновлена

## 🆘 Если забыли пароли

Если вы забыли пароли keystore, нужно создать новый:

```bash
./create_keystore.sh
```

**Внимание**: Старый keystore нельзя использовать для обновления приложения в Play Store, если вы его потеряли!

