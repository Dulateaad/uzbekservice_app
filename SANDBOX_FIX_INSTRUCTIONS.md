# 🔧 Инструкции по исправлению Sandbox ошибок

## ❌ Проблема:
```
error: Sandbox: dartvm deny file-read-data
error: Sandbox: rsync deny file-read-data
```

Это системное ограничение macOS, которое блокирует доступ к файлам проекта.

## ✅ Решения (по приоритету):

### 🥇 Решение 1: Добавить Xcode в Full Disk Access (РЕКОМЕНДУЕТСЯ)

1. Откройте **System Settings** (Системные настройки)
2. Перейдите в **Privacy & Security** (Конфиденциальность и безопасность)
3. Найдите **Full Disk Access** (Полный доступ к диску)
4. Нажмите замок 🔒 внизу для разблокировки (введите пароль)
5. Нажмите кнопку **+** (плюс)
6. Найдите и добавьте **Xcode** (обычно в `/Applications/Xcode.app`)
7. Убедитесь, что переключатель рядом с Xcode **ВКЛЮЧЕН** ✅
8. **Перезапустите Xcode** (полностью закройте и откройте снова)

### 🥈 Решение 2: Добавить Terminal в Full Disk Access

Если используете `flutter build` из терминала:

1. Откройте **System Settings** → **Privacy & Security** → **Full Disk Access**
2. Нажмите **+** и добавьте **Terminal**
3. Включите переключатель
4. **Перезапустите Terminal**

### 🥉 Решение 3: Сборка через Xcode напрямую (ОБХОДИТ Sandbox)

Этот метод часто обходит все Sandbox проблемы:

1. **Откройте проект в Xcode:**
   ```bash
   open ~/uzbekservice_app/ios/Runner.xcworkspace
   ```

2. **Выберите устройство:**
   - В верхней панели Xcode выберите **"Any iOS Device"** (не симулятор!)

3. **Настройте схему:**
   - **Product** → **Scheme** → **Edit Scheme...**
   - В левой колонке выберите **"Archive"**
   - **Build Configuration**: выберите **"Release"**
   - Нажмите **Close**

4. **Создайте Archive:**
   - **Product** → **Archive**
   - Дождитесь завершения (может занять 5-15 минут)

5. **Загрузите в TestFlight:**
   - После завершения откроется **Organizer**
   - Нажмите **Distribute App**
   - Выберите **App Store Connect**
   - Следуйте инструкциям

### 🔄 Решение 4: Перезапустить Mac

Иногда помогает простой перезапуск:

1. Сохраните все работы
2. Перезапустите Mac
3. После перезапуска попробуйте сборку снова

### 🧹 Решение 5: Очистить и пересобрать

```bash
cd ~/uzbekservice_app
flutter clean
cd ios
pod install
cd ..
flutter build ios --release
```

## 🔍 Диагностика:

### Проверить логи Sandbox:

1. Откройте **Console.app** (в Applications → Utilities)
2. В поиске введите: `sandbox`
3. Посмотрите логи - должны быть разрешения для Xcode

### Проверить версию macOS:

```bash
sw_vers
```

Некоторые версии macOS (особенно beta) имеют более строгие Sandbox правила.

## ⚠️ Важно:

- **Xcode должен быть в Full Disk Access** - это самое важное!
- После добавления в Full Disk Access **обязательно перезапустите Xcode**
- Если используете Terminal для сборки, добавьте и Terminal тоже

## ✅ После применения решений:

- ✅ Sandbox ошибки должны исчезнуть
- ✅ Сборка должна пройти успешно
- ✅ Можно создавать Archive

## 💡 Рекомендация:

**Начните с Решения 1** (добавить Xcode в Full Disk Access) - это решает проблему в 90% случаев.

Если не помогает, попробуйте **Решение 3** (сборка через Xcode напрямую) - это часто обходит все Sandbox проблемы.

---

**Скрипт для быстрой проверки:**
```bash
./fix_sandbox.sh
```

