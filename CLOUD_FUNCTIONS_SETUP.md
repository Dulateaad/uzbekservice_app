# ☁️ Настройка Cloud Functions для автоматической отправки push-уведомлений

## 📋 Что делает каждая функция

### 1. `sendOrderNotification`
- **Триггер**: Создание нового заказа в Firestore
- **Действие**: Отправляет уведомление специалисту о новом заказе
- **Данные**: `{ type: 'order', orderId: '...', status: 'pending' }`

### 2. `sendChatNotification`
- **Триггер**: Создание нового сообщения в чате
- **Действие**: Отправляет уведомление получателю о новом сообщении
- **Данные**: `{ type: 'chat', chatId: '...', senderId: '...', messageId: '...' }`

### 3. `sendOrderStatusNotification`
- **Триггер**: Изменение статуса заказа
- **Действие**: Отправляет уведомление при изменении статуса (принят, выполнен, отменен)
- **Данные**: `{ type: 'order', orderId: '...', status: '...' }`

### 4. `sendReviewNotification`
- **Триггер**: Создание нового отзыва
- **Действие**: Отправляет уведомление специалисту о новом отзыве
- **Данные**: `{ type: 'specialist', specialistId: '...', reviewId: '...', action: 'new_review' }`

## 🚀 Установка и развертывание

### Шаг 1: Установите зависимости

```bash
cd ~/uzbekservice_app/functions
npm install
```

### Шаг 2: Проверьте настройки Firebase

Убедитесь, что вы авторизованы в Firebase CLI:

```bash
firebase login
```

Проверьте, что проект выбран правильно:

```bash
firebase use odo-uz-1f4d9
```

### Шаг 3: Разверните функции

```bash
# Развернуть все функции
firebase deploy --only functions

# Или развернуть конкретную функцию
firebase deploy --only functions:sendOrderNotification
```

### Шаг 4: Проверьте логи

```bash
# Просмотр логов в реальном времени
firebase functions:log

# Просмотр логов конкретной функции
firebase functions:log --only sendOrderNotification
```

## 🧪 Тестирование

### Локальное тестирование (эмулятор)

```bash
# Запустить эмулятор
npm run serve

# В другом терминале - создать тестовый заказ
# Функция автоматически сработает в эмуляторе
```

### Тестирование в продакшене

1. Создайте тестовый заказ в Firestore через Firebase Console
2. Проверьте логи: `firebase functions:log`
3. Проверьте, что уведомление получено на устройстве

## 📊 Мониторинг

### Просмотр метрик в Firebase Console

1. Откройте: https://console.firebase.google.com/project/odo-uz-1f4d9/functions
2. Просмотрите:
   - Количество вызовов
   - Время выполнения
   - Ошибки

### Настройка алертов

В Firebase Console → Functions → Settings можно настроить алерты:
- При ошибках выполнения
- При превышении времени выполнения
- При большом количестве вызовов

## 🔧 Настройка

### Изменение текста уведомлений

Отредактируйте файл `functions/index.js`:

```javascript
notification: {
  title: 'Ваш заголовок',
  body: 'Ваш текст',
}
```

### Добавление новых функций

1. Добавьте новую функцию в `functions/index.js`
2. Установите зависимости: `npm install`
3. Разверните: `firebase deploy --only functions`

## 💰 Стоимость

Cloud Functions входят в бесплатный тариф Firebase:
- Первые 2 миллиона вызовов в месяц - бесплатно
- После этого: $0.40 за миллион вызовов

Для большинства приложений этого более чем достаточно.

## 🐛 Отладка

### Проблема: Функции не срабатывают

1. Проверьте логи: `firebase functions:log`
2. Убедитесь, что функции развернуты: `firebase functions:list`
3. Проверьте правила Firestore - функции должны иметь доступ к данным

### Проблема: Уведомления не отправляются

1. Проверьте, что у пользователя есть токены в `deviceTokens`
2. Проверьте настройки уведомлений: `notificationPreferences.push === true`
3. Проверьте логи функции на ошибки

### Проблема: Токены не удаляются

Функции автоматически удаляют невалидные токены. Если проблема сохраняется:
1. Проверьте логи на ошибки обновления Firestore
2. Убедитесь, что правила Firestore разрешают обновление `deviceTokens`

## 📚 Дополнительные ресурсы

- [Firebase Cloud Functions Documentation](https://firebase.google.com/docs/functions)
- [Firebase Cloud Messaging](https://firebase.google.com/docs/cloud-messaging)
- [Firestore Triggers](https://firebase.google.com/docs/functions/firestore-events)

