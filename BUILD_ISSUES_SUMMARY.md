# 🔧 Проблемы сборки AAB - Резюме

## ✅ Исправлено:

1. **build.gradle.kts** - добавлены импорты, исправлен синтаксис
2. **NDK** - переустановлен
3. **uni_links** - добавлен namespace
4. **google_maps_service.dart** - разделен на веб и мобильные версии

## ❌ Осталось исправить:

### Файлы с веб-специфичным кодом:

1. **lib/screens/maps/maps_screen.dart** (строка 30)
   - Использует: `html.window.navigator.geolocation`
   - Решение: Использовать `geolocator` для мобильных

2. **lib/widgets/simple_google_maps_widget.dart** (строки 46, 49)
   - Использует: `ui_web.platformViewRegistry`, `html.DivElement`
   - Решение: Использовать условную компиляцию или `google_maps_flutter` виджет

## 🚀 Быстрое решение:

### Вариант 1: Временно исключить веб-специфичные экраны

Добавить в `android/app/build.gradle.kts`:
```kotlin
android {
    // ...
    sourceSets {
        main {
            java {
                exclude '**/maps/**'
                exclude '**/simple_google_maps_widget.dart'
            }
        }
    }
}
```

### Вариант 2: Исправить файлы (рекомендуется)

1. **maps_screen.dart**: Заменить `html.window` на `geolocator`
2. **simple_google_maps_widget.dart**: Использовать условную компиляцию

## 📝 Следующие шаги:

1. Исправить оставшиеся веб-специфичные файлы
2. Пересобрать AAB
3. Протестировать на устройстве

## 💡 Рекомендация:

Для Android используйте `google_maps_flutter` виджет вместо веб-версии Google Maps.

