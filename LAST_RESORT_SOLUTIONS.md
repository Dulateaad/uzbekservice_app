# 🔧 Последние решения Sandbox проблем

## ❌ Проблема:

Sandbox ошибки продолжаются даже при Archive и с новым путем DerivedData. Это системное ограничение macOS 26.1.

## ✅ Последние решения:

### 🥇 Решение 1: Исправить права доступа через sudo

**ВНИМАНИЕ:** Требует пароль администратора

Выполните в терминале:

```bash
# Исправить права на DerivedData
sudo chown -R $(whoami) ~/XcodeDerivedData/
sudo chmod -R u+w ~/XcodeDerivedData/

# Исправить права на проект
sudo chown -R $(whoami) ~/uzbekservice_app/ios/
sudo chmod -R u+w ~/uzbekservice_app/ios/

# Исправить права на build директорию
sudo chown -R $(whoami) ~/uzbekservice_app/build/
sudo chmod -R u+w ~/uzbekservice_app/build/
```

После выполнения:
1. Полностью закройте Xcode (`Cmd + Q`)
2. Откройте проект заново
3. Попробуйте Archive снова

### 🥈 Решение 2: Перезапустить Mac

Иногда помогает полный перезапуск системы:

1. Сохраните все работы
2. Полностью перезапустите Mac
3. После перезапуска:
   - Откройте Xcode
   - Попробуйте Archive снова

**Почему это работает:** Перезапуск сбрасывает все Sandbox сессии и разрешения.

### 🥉 Решение 3: Вернуть DerivedData на Default

Попробуйте вернуть DerivedData на стандартный путь:

1. **Xcode → Settings** (`Cmd + ,`)
2. **Locations** → **Derived Data**
3. Выберите **"Default"** вместо "Custom"
4. Перезапустите Xcode
5. Попробуйте Archive снова

### 🔧 Решение 4: Сборка через командную строку

Попробуйте сборку через xcodebuild напрямую:

```bash
cd ~/uzbekservice_app/ios
xcodebuild -workspace Runner.xcworkspace \
  -scheme Runner \
  -configuration Release \
  -destination 'generic/platform=iOS' \
  archive \
  -archivePath ~/Desktop/Runner.xcarchive
```

### 🔄 Решение 5: Использовать другой Mac

Если ничего не помогает:
- Используйте другой Mac для сборки
- Или обновите macOS до стабильной версии (не beta)

## 💡 Рекомендация:

**Начните с Решения 1** (исправить права через sudo) - это часто решает проблему.

Если не помогает, попробуйте **Решение 2** (перезапуск Mac).

## ⚠️ Важно:

- **macOS 26.1** (ваша версия) - это очень новая версия, возможно beta
- Beta версии macOS часто имеют проблемы с Sandbox
- После исправления прав **обязательно перезапустите Xcode**

## 🔍 Диагностика:

Проверьте логи Sandbox:

1. Откройте **Console.app** (Applications → Utilities)
2. В поиске введите: `sandbox`
3. Посмотрите логи - должны быть разрешения для Xcode

---

**Скрипт для быстрого запуска:**
```bash
./fix_permissions.sh
```

