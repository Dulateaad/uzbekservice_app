# 🚀 Быстрый старт: Настройка Firebase Storage

## Шаг 1: Развернуть правила безопасности

```bash
cd ~/uzbekservice_app
./deploy_firestore.sh
```

Выберите вариант **4** (Только правила Storage) или **5** (Все).

Или вручную:

```bash
firebase deploy --only storage
```

## Шаг 2: Проверить в Firebase Console

Откройте [Firebase Console - Storage](https://console.firebase.google.com/project/odo-uz-1f4d9/storage/rules)

## Шаг 3: Структура папок

Storage автоматически создаст структуру при первой загрузке:

```
avatars/{userId}/          - Аватары пользователей
orders/{orderId}/          - Фотографии заказов
specialists/{id}/documents/ - Документы специалистов
temp/{userId}/            - Временные файлы
public/                   - Публичные файлы
```

## 📝 Пример использования

```dart
import 'package:firebase_storage/firebase_storage.dart';

// Загрузка аватара
final storage = FirebaseStorage.instance;
final ref = storage.ref('avatars/$userId/avatar.jpg');
await ref.putFile(imageFile);
final url = await ref.getDownloadURL();
```

## ⚠️ Важно

- **Максимальный размер файла**: 10 МБ
- **Разрешенные типы**: изображения (image/*), документы (PDF, DOC, DOCX)
- **Безопасность**: Все операции требуют аутентификации (кроме public/)

## 📚 Подробная документация

См. [STORAGE_SETUP.md](./STORAGE_SETUP.md) для подробной информации.

