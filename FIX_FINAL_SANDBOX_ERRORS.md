# 🔧 Исправление Sandbox ошибок в конце сборки

## ❌ Ошибки:
```
Sandbox: rsync deny file-read-data
Sandbox: dartvm deny file-write-create
/Users/dulatea/uzbekservice_app/build/ios/Release-iphoneos
```

## ✅ Причина:
Xcode не может записать файлы в папку `build/ios/Release-iphoneos` из-за Sandbox ограничений.

## 🔧 Решение:

### Вариант 1: Сборка через Xcode (рекомендуется)

Этот метод часто обходит Sandbox проблемы:

1. **Откройте Xcode:**
   ```bash
   open ~/uzbekservice_app/ios/Runner.xcworkspace
   ```

2. **Выберите устройство:**
   - В верхней панели: **"Any iOS Device"** (не симулятор!)

3. **Очистите проект:**
   - **Product** → **Clean Build Folder** (`Shift + Cmd + K`)

4. **Попробуйте сборку:**
   - **Product** → **Build** (`Cmd + B`)

5. **После успешной сборки:**
   - **Product** → **Archive**

### Вариант 2: Дайте Xcode Full Disk Access

1. **System Settings** → **Privacy & Security**
2. **Full Disk Access**
3. Убедитесь, что **Xcode** включен
4. Если нет:
   - Нажмите **+**
   - Найдите **Xcode** в `/Applications`
   - Добавьте и включите
5. **Перезапустите Xcode**

### Вариант 3: Очистите и пересоберите

```bash
cd ~/uzbekservice_app
rm -rf build/ios/Release-iphoneos
flutter clean
flutter pub get
flutter build ios --release
```

### Вариант 4: Исправьте права на папку

```bash
cd ~/uzbekservice_app
chmod -R 755 build
chmod -R 755 ios
```

Затем попробуйте снова:
```bash
flutter build ios --release
```

## 🔍 Альтернативное решение:

### Сборка для симулятора (для тестирования):

```bash
flutter build ios --simulator
```

Это обходит Sandbox проблемы, но не подходит для TestFlight.

## ✅ После исправления:

- ✅ Sandbox ошибки должны исчезнуть
- ✅ Сборка должна завершиться успешно
- ✅ Можно создавать Archive

## 📱 После успешной сборки:

1. **Product** → **Archive**
2. **Organizer** → **Distribute App**
3. **App Store Connect** → **Upload**
4. Загрузка в TestFlight

## 💡 Рекомендация:

**Используйте Вариант 1** (сборка через Xcode) - это самый надежный способ для создания Archive для TestFlight.

