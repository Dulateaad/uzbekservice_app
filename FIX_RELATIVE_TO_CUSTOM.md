# 🔧 Исправление: Вернуть Custom вместо Relative

## ❌ Проблема:

Настройка изменилась с "Custom" на "Relative", и путь стал:
```
/DerivedData
```

Это **неправильно** - нужно вернуть "Custom" с полным путем!

## ✅ Решение:

### Шаг 1: Измените настройку обратно на Custom

В диалоге "Locations" в разделе **"Derived Data"**:

1. **Кликните на "Relative"** (или на путь `/DerivedData`)
2. В открывшемся меню выберите **"Custom"**
3. В текстовом поле введите полный путь:
   ```
   /Users/dulatea/XcodeDerivedData
   ```
4. Нажмите **"Done"**

### Шаг 2: Проверьте настройку

После изменения должно быть:
```
Derived Data
/Users/dulatea/XcodeDerivedData
[Custom] [Build Location...]
```

**НЕ должно быть:**
```
Derived Data
/DerivedData
[Relative] [Build Location...]
```

### Шаг 3: Закройте и перезапустите Xcode

1. Закройте окно **"Locations"** (нажмите "Done")
2. **Полностью закройте Xcode** (`Cmd + Q`)
3. Откройте проект заново:
   ```bash
   open ~/uzbekservice_app/ios/Runner.xcworkspace
   ```

### Шаг 4: Проверьте настройки после перезапуска

1. **Xcode → Settings** (`Cmd + ,`)
2. Перейдите на вкладку **"Locations"**
3. Убедитесь, что:
   ```
   Derived Data: Custom
   /Users/dulatea/XcodeDerivedData
   ```

### Шаг 5: Очистите и пересоберите

1. **Product → Clean Build Folder** (`Shift + Cmd + K`)
2. **Product → Build** (`Cmd + B`)

## ⚠️ Важно:

- **"Relative"** - это относительный путь (неправильно для нашей задачи)
- **"Custom"** - это абсолютный путь (правильно)
- Путь должен быть полным: `/Users/dulatea/XcodeDerivedData`

## 💡 Почему это важно:

"Relative" использует относительный путь, который может вызывать проблемы с Sandbox. "Custom" с абсолютным путем в домашней директории обходит эти проблемы.

