# Установка CocoaPods

## ✅ Файл GoogleService-Info.plist уже скопирован!

Теперь нужно установить CocoaPods для установки зависимостей iOS.

## 🚀 Способ 1: Через Homebrew (рекомендуется, без sudo)

```bash
# Установить Homebrew (если еще не установлен)
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установить CocoaPods
brew install cocoapods
```

## 🔧 Способ 2: Через gem (требует sudo)

```bash
sudo gem install cocoapods
```

Вам будет предложено ввести пароль администратора.

## 📦 Способ 3: Пользовательская установка (без sudo)

```bash
# Установить в пользовательскую директорию
gem install --user-install cocoapods

# Добавить в PATH (добавьте в ~/.zshrc)
export PATH="$HOME/.gem/ruby/2.6.0/bin:$PATH"
source ~/.zshrc
```

## ✅ После установки

Проверьте установку:
```bash
pod --version
```

Должно показать версию, например: `1.15.2`

Затем установите зависимости:
```bash
cd ~/uzbekservice_app/ios
pod install
cd ..
```

## 🐛 Если возникают проблемы

### Проблема: "pod: command not found"
**Решение**: Убедитесь, что CocoaPods установлен и в PATH:
```bash
which pod
```

### Проблема: "Unable to find a specification"
**Решение**: Обновите репозиторий:
```bash
pod repo update
```

### Проблема: Ошибки при установке
**Решение**: Очистите кеш и переустановите:
```bash
cd ios
rm -rf Pods Podfile.lock
pod cache clean --all
pod install
```

## 📝 Быстрая проверка

После установки CocoaPods выполните:
```bash
cd ~/uzbekservice_app/ios
pod install
cd ..
flutter pub get
```

Затем проверьте:
```bash
ls -la ios/Runner/GoogleService-Info.plist
ls -la ios/Pods
```

Оба должны существовать.

