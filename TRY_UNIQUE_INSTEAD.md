# 🔄 Альтернативное решение: Использовать "Unique" вместо "Custom"

## ❌ Проблема:

Настройка "Custom" не сохраняется и возвращается к "Relative" после перезапуска.

## ✅ Решение: Использовать "Unique"

Вместо "Custom" попробуйте использовать **"Unique"** - это работает стабильнее:

### Шаг 1: В настройках Xcode

1. **Xcode → Settings** (`Cmd + ,`)
2. Перейдите на вкладку **"Locations"**
3. В разделе **"Derived Data"**:
   - Выберите **"Default"** (если сейчас "Relative")
   - Нажмите **"Build Location..."**
   - В диалоге "Build Location" выберите **"Unique"**
   - Нажмите **"Done"**

### Шаг 2: Или установите через defaults

```bash
defaults write com.apple.dt.Xcode IDECustomBuildLocationType "Unique"
```

### Шаг 3: Перезапустите Xcode

1. **Полностью закройте Xcode** (`Cmd + Q`)
2. Откройте проект заново

### Шаг 4: Проверьте

После перезапуска, в настройках Locations → Derived Data должно быть:
```
Derived Data: Default
```

А в Build Location должно быть:
```
Build Location: Unique
```

## 💡 Почему "Unique" лучше:

- Работает стабильнее, чем "Custom"
- Автоматически создает правильные пути
- Не требует ручного указания путей
- Меньше проблем с сохранением настроек

## 🔧 Если все еще не работает:

Попробуйте перезапустить Mac - иногда это помогает с настройками Xcode.

