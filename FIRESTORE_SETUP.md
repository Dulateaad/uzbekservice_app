# 🔥 Настройка Firestore для ODO.UZ

## 📋 Содержание

1. [Структура данных](#структура-данных)
2. [Правила безопасности](#правила-безопасности)
3. [Индексы](#индексы)
4. [Развертывание](#развертывание)
5. [Тестирование](#тестирование)

## 📊 Структура данных

### Коллекция `users`

Документы пользователей (клиенты и специалисты).

**Поля:**
```typescript
{
  id: string                    // ID пользователя (совпадает с Firebase Auth UID)
  phoneNumber: string           // Номер телефона
  name: string                  // Имя пользователя
  userType: 'client' | 'specialist'
  email?: string                // Email (опционально)
  oneIdSub?: string             // OneID идентификатор
  category?: string             // Категория специалиста (только для специалистов)
  description?: string          // Описание (только для специалистов)
  pricePerHour?: number        // Цена за час (только для специалистов)
  avatarUrl?: string            // URL аватара
  deviceTokens?: string[]       // Токены для push-уведомлений
  notificationPreferences?: {   // Настройки уведомлений
    push: boolean
    sms: boolean
    email: boolean
  }
  createdAt: Timestamp
  updatedAt: Timestamp
  isVerified: boolean           // Верифицирован ли пользователь
  location?: {                  // Геолокация
    lat: number
    lng: number
  }
  skills?: string[]             // Навыки (только для специалистов)
  rating?: number               // Средний рейтинг (0-5)
  totalOrders?: number          // Количество заказов
}
```

**Подколлекция `services`** (только для специалистов):
```typescript
{
  id: string
  specialistId: string
  name: string
  description?: string
  price: number
  durationMinutes: number
  category?: string
  isActive: boolean
  createdAt: Timestamp
  updatedAt: Timestamp
}
```

### Коллекция `orders`

Заказы клиентов.

**Поля:**
```typescript
{
  id: string
  clientId: string              // ID клиента
  specialistId: string          // ID специалиста
  category: string              // Категория услуги
  title: string                 // Название заказа
  description: string            // Описание заказа
  status: 'pending' | 'accepted' | 'in_progress' | 'completed' | 'cancelled'
  price: number                 // Общая стоимость
  address?: string               // Адрес оказания услуг
  location?: {                   // Координаты
    lat: number
    lng: number
  }
  scheduledDate: Timestamp      // Запланированная дата
  timeSlot?: string             // Временной слот (например, "10:00-12:00")
  totalDurationMinutes: number  // Общая длительность в минутах
  services: OrderServiceItem[]  // Список услуг
  completedAt?: Timestamp       // Дата завершения
  createdAt: Timestamp
  updatedAt: Timestamp
  images?: string[]              // Фотографии
  notes?: string                // Заметки
  rating?: number               // Рейтинг (1-5)
  review?: string                // Отзыв
}
```

### Коллекция `reviews`

Отзывы клиентов о специалистах.

**Поля:**
```typescript
{
  id: string
  orderId: string               // ID заказа
  clientId: string              // ID клиента
  specialistId: string          // ID специалиста
  rating: number                // Рейтинг (1-5)
  comment: string               // Текст отзыва
  createdAt: Timestamp
}
```

### Коллекция `chats`

Чаты между клиентами и специалистами.

**Поля:**
```typescript
{
  id: string
  clientId: string
  specialistId: string
  orderId?: string              // Связанный заказ (опционально)
  lastMessage: string            // Последнее сообщение
  lastMessageTime: Timestamp
  createdAt: Timestamp
  updatedAt: Timestamp
}
```

**Подколлекция `messages`:**
```typescript
{
  id: string
  senderId: string              // ID отправителя
  text: string                  // Текст сообщения
  timestamp: Timestamp
  read: boolean                 // Прочитано ли сообщение
}
```

## 🔒 Правила безопасности

Правила безопасности находятся в файле `firestore.rules`.

### Основные принципы:

1. **Аутентификация**: Все операции требуют аутентификации
2. **Владелец**: Пользователь может изменять только свои данные
3. **Роли**: Разные права для клиентов и специалистов
4. **Валидация**: Проверка типов данных и значений

### Права доступа:

- **Пользователи**: Могут читать свой профиль и профили специалистов, обновлять только свой профиль
- **Специалисты**: Могут управлять своими услугами
- **Заказы**: Клиент создает, оба участника могут обновлять статус
- **Отзывы**: Только клиенты могут создавать отзывы на завершенные заказы
- **Чаты**: Только участники чата могут читать и отправлять сообщения

## 📇 Индексы

Индексы находятся в файле `firestore.indexes.json`.

### Созданные индексы:

1. **users**: `userType` + `rating` (для списка специалистов)
2. **users**: `userType` + `category` + `rating` (для фильтрации по категориям)
3. **orders**: `clientId` + `createdAt` (для истории заказов клиента)
4. **orders**: `specialistId` + `createdAt` (для заказов специалиста)
5. **orders**: `status` + `createdAt` (для фильтрации по статусу)
6. **orders**: `status` + `createdAt` + `scheduledDate` (для планирования)
7. **orders**: `status` + `category` + `createdAt` (для аналитики)
8. **reviews**: `specialistId` + `createdAt` (для отзывов специалиста)
9. **chats**: `clientId` + `lastMessageTime` (для списка чатов клиента)
10. **chats**: `specialistId` + `lastMessageTime` (для списка чатов специалиста)

## 🚀 Развертывание

### 1. Развернуть правила безопасности

```bash
cd ~/uzbekservice_app
firebase deploy --only firestore:rules
```

### 2. Развернуть индексы

```bash
firebase deploy --only firestore:indexes
```

**Примечание**: Создание индексов может занять несколько минут. Проверьте статус в [Firebase Console](https://console.firebase.google.com/project/odo-uz-1f4d9/firestore/indexes).

### 3. Развернуть все сразу

```bash
firebase deploy --only firestore
```

## 🧪 Тестирование

### Проверка правил безопасности

1. Откройте [Firebase Console](https://console.firebase.google.com/project/odo-uz-1f4d9/firestore/rules)
2. Используйте симулятор правил для тестирования различных сценариев

### Тестовые сценарии:

#### ✅ Успешные операции:

1. **Клиент создает свой профиль**
   - Аутентифицированный пользователь
   - `userId` совпадает с `auth.uid`
   - Все обязательные поля заполнены

2. **Специалист создает услугу**
   - Пользователь является специалистом
   - `specialistId` совпадает с `userId`

3. **Клиент создает заказ**
   - Пользователь является клиентом
   - `clientId` совпадает с `auth.uid`
   - Статус = 'pending'

4. **Специалист принимает заказ**
   - Пользователь является специалистом
   - `specialistId` совпадает с `auth.uid`
   - Статус меняется на 'accepted'

#### ❌ Запрещенные операции:

1. **Клиент не может изменить userType**
   - Правило проверяет, что `userType` не изменяется

2. **Нельзя удалить заказ**
   - Правило `allow delete: if false;`

3. **Нельзя изменить сообщение**
   - Сообщения неизменяемы

## 📝 Важные замечания

1. **Безопасность**: Правила безопасности критически важны. Всегда тестируйте их перед развертыванием в production.

2. **Индексы**: Если вы получаете ошибку о недостающем индексе, Firebase предложит создать его автоматически. Следуйте ссылке в ошибке.

3. **Производительность**: Используйте индексы для всех запросов с фильтрами и сортировкой.

4. **Мониторинг**: Регулярно проверяйте использование Firestore в Firebase Console для оптимизации затрат.

## 🔗 Полезные ссылки

- [Firebase Console](https://console.firebase.google.com/project/odo-uz-1f4d9)
- [Документация Firestore Security Rules](https://firebase.google.com/docs/firestore/security/get-started)
- [Документация Firestore Indexes](https://firebase.google.com/docs/firestore/query-data/indexing)
