# 🔧 Исправление Sandbox ошибок доступа

## ❌ Ошибки:
```
Sandbox: dartvm deny file-read-data
Sandbox: rsync deny file-read-data
Sandbox: dartvm deny file-write-create
```

## ✅ Решение:

### Шаг 1: Удалите Derived Data

```bash
rm -rf ~/Library/Developer/Xcode/DerivedData/*
```

### Шаг 2: Проверьте права доступа macOS

1. **System Settings** → **Privacy & Security**
2. **Full Disk Access**
3. Убедитесь, что **Xcode** имеет доступ
4. Если нет, добавьте Xcode в список

### Шаг 3: Очистите проект

```bash
cd ~/uzbekservice_app
flutter clean
flutter pub get
```

### Шаг 4: В Xcode

1. **Product** → **Clean Build Folder** (`Shift + Cmd + K`)

2. **Закройте Xcode полностью** (`Cmd + Q`)

3. **Откройте снова:**
   ```bash
   open ~/uzbekservice_app/ios/Runner.xcworkspace
   ```

4. **Product** → **Build** (`Cmd + B`)

### Шаг 5: Альтернатива - Сборка через Flutter

Если Sandbox ошибки продолжаются, попробуйте собрать через Flutter:

```bash
cd ~/uzbekservice_app
flutter build ios --release
```

Затем в Xcode:
- **Product** → **Archive**

## 🔍 Дополнительные решения:

### Вариант 1: Перезапустите Mac

Иногда помогает полный перезапуск системы.

### Вариант 2: Проверьте права на файлы

```bash
# Проверьте права
ls -la ~/uzbekservice_app/ios/Runner/

# Если нужно, исправьте
chmod -R 755 ~/uzbekservice_app
```

### Вариант 3: Отключите Sandbox (не рекомендуется)

Это не рекомендуется, но можно попробовать:
- В Build Settings найдите "Enable App Sandbox"
- Отключите для Debug конфигурации

### Вариант 4: Используйте другой метод сборки

```bash
# Соберите через Flutter
flutter build ios --release

# Затем в Xcode только Archive
```

## ✅ После исправления:

- ✅ Sandbox ошибки должны исчезнуть
- ✅ Статус: "Build Succeeded"
- ✅ Можно создавать Archive

## 📱 После успешной сборки:

1. **Product** → **Archive**
2. **Organizer** → **Distribute App**
3. **App Store Connect** → **Upload**
4. Загрузка в TestFlight

## 💡 Профилактика:

- Регулярно очищайте Derived Data
- Убедитесь, что Xcode имеет Full Disk Access
- Используйте `flutter clean` перед важными сборками

