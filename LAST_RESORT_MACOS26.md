# 🚨 Последние решения для macOS 26.1 Sandbox проблем

## ❌ Проблема:

Sandbox ошибки продолжаются даже со стандартным путем DerivedData. Это **системное ограничение macOS 26.1** (возможно beta версия).

## ✅ Решение 1: Исправить права + Перезапуск Mac (НАИБОЛЕЕ ЭФФЕКТИВНО)

### Шаг 1: Исправьте права на стандартный DerivedData

Выполните в терминале (требует пароль):

```bash
sudo chown -R $(whoami) ~/Library/Developer/Xcode/DerivedData/
sudo chmod -R u+w ~/Library/Developer/Xcode/DerivedData/
```

### Шаг 2: Полностью перезапустите Mac

**Это критически важно!** Перезапуск сбрасывает все Sandbox сессии:

1. Сохраните все работы
2. Полностью перезапустите Mac (не просто выход из системы)
3. После перезапуска:
   - Откройте Xcode
   - Проверьте, что DerivedData = Default
   - Попробуйте сборку снова

**Почему это работает:** Перезапуск полностью сбрасывает Sandbox сессии и разрешения macOS.

## ✅ Решение 2: Проверить настройки безопасности macOS

1. **System Settings** → **Privacy & Security**
2. Проверьте раздел **"Full Disk Access"**
3. Убедитесь, что добавлены:
   - **Xcode**
   - **Terminal** (или iTerm)
   - **Flutter** (если есть)

4. Если не добавлены - добавьте и перезапустите Mac

## ✅ Решение 3: Сборка через xcodebuild с явными путями

Попробуйте сборку через xcodebuild напрямую:

```bash
cd ~/uzbekservice_app/ios

# Очистить
xcodebuild clean -workspace Runner.xcworkspace -scheme Runner

# Собрать Archive
xcodebuild archive \
  -workspace Runner.xcworkspace \
  -scheme Runner \
  -configuration Release \
  -destination 'generic/platform=iOS' \
  -archivePath ~/Desktop/Runner.xcarchive \
  -derivedDataPath ~/Library/Developer/Xcode/DerivedData \
  CODE_SIGN_IDENTITY="" \
  CODE_SIGNING_REQUIRED=NO
```

**Примечание:** Для загрузки в App Store нужна кодовая подпись, но для создания Archive можно обойти.

## ✅ Решение 4: Использовать другой Mac

Если ничего не помогает:

- Используйте другой Mac для сборки
- Или обновите macOS до стабильной версии (не beta)

## ✅ Решение 5: Сборка на CI/CD (GitHub Actions, Bitrise)

Если локальная сборка не работает:

- Используйте CI/CD сервисы для сборки
- Они часто обходят локальные Sandbox проблемы
- GitHub Actions предоставляет бесплатные минуты для open source проектов

## 🔍 Диагностика:

### Проверьте версию macOS:

```bash
sw_vers
```

Если версия **26.1** - это очень новая версия, возможно beta.

### Проверьте логи Sandbox:

1. Откройте **Console.app** (Applications → Utilities)
2. В поиске введите: `sandbox`
3. Посмотрите логи - должны быть разрешения для Xcode

### Проверьте настройки Xcode:

```bash
defaults read com.apple.dt.Xcode
```

## 💡 Рекомендация:

**Начните с Решения 1** (исправить права + перезапуск Mac) - это часто решает проблему в beta версиях macOS.

## ⚠️ Важно:

- **macOS 26.1** - это очень новая версия, возможно beta
- Beta версии macOS часто имеют проблемы с Sandbox
- Перезапуск Mac часто решает эти проблемы
- Если проблема критична - рассмотрите использование стабильной версии macOS

## 📋 Чеклист:

- [ ] Исправлены права на DerivedData
- [ ] Добавлены Xcode и Terminal в Full Disk Access
- [ ] Перезапущен Mac
- [ ] Проверено, что DerivedData = Default
- [ ] Попробована сборка снова

---

**Если ничего не помогает - рассмотрите использование CI/CD или другого Mac для сборки.**

