# 🧪 Тестирование и сборка релизной версии

## 📋 Чек-лист перед сборкой

### 1. Проверка конфигурации

- [x] Firebase настроен (Auth, Firestore, Storage)
- [x] Правила безопасности развернуты
- [x] Storage интегрирован
- [ ] Keystore создан
- [ ] key.properties настроен
- [ ] Версия в pubspec.yaml обновлена

### 2. Тестирование основных функций

#### Авторизация
- [ ] Регистрация через телефон
- [ ] Регистрация через OneID
- [ ] Вход в систему
- [ ] Выход из системы

#### Профиль
- [ ] Просмотр профиля
- [ ] Редактирование профиля
- [ ] Загрузка аватара
- [ ] Обновление информации

#### Поиск и заказы
- [ ] Поиск специалистов
- [ ] Фильтрация по категориям
- [ ] Просмотр профиля специалиста
- [ ] Создание заказа
- [ ] Выбор услуг
- [ ] Выбор даты и времени
- [ ] Указание адреса
- [ ] Подтверждение заказа

#### Оплата
- [ ] Выбор способа оплаты
- [ ] Оплата через Click
- [ ] Проверка успешной оплаты

#### Чат
- [ ] Открытие чата
- [ ] Отправка сообщений
- [ ] Получение сообщений

#### Дополнительно
- [ ] История заказов
- [ ] Избранные специалисты
- [ ] Рейтинг по городам (для специалистов)

## 🔨 Сборка релизной версии

### Шаг 1: Создание Keystore

```bash
cd ~/uzbekservice_app
./create_keystore.sh
```

**Важно**: Сохраните пароли в безопасном месте!

### Шаг 2: Настройка key.properties

Откройте `android/key.properties` и замените плейсхолдеры:

```properties
storePassword=ВАШ_ПАРОЛЬ_КЕЙСТОРА
keyPassword=ВАШ_ПАРОЛЬ_КЛЮЧА
keyAlias=odo_uz_key
storeFile=../odo_uz_keystore.jks
```

### Шаг 3: Обновление версии

В `pubspec.yaml`:
```yaml
version: 1.0.0+1
```
- `1.0.0` - версия для пользователей
- `1` - номер версии для Play Store

При каждом обновлении увеличивайте оба числа:
- `1.0.1+2`
- `1.0.2+3`
- и т.д.

### Шаг 4: Сборка AAB

```bash
./build_release.sh
```

Или вручную:
```bash
flutter clean
flutter pub get
flutter build appbundle --release
```

### Шаг 5: Проверка сборки

Файл будет в: `build/app/outputs/bundle/release/app-release.aab`

Проверьте размер файла (должен быть < 150 МБ для Play Store).

## 🧪 Тестирование релизной сборки

### Установка на устройство

```bash
# Создать APK из AAB для тестирования
bundletool build-apks \
  --bundle=build/app/outputs/bundle/release/app-release.aab \
  --output=app-release.apks \
  --ks=android/odo_uz_keystore.jks \
  --ks-pass=pass:ВАШ_ПАРОЛЬ \
  --ks-key-alias=odo_uz_key \
  --key-pass=pass:ВАШ_ПАРОЛЬ
```

Или установите через Android Studio или adb.

### Что проверить:

1. **Запуск приложения** - приложение должно запускаться без ошибок
2. **Авторизация** - вход должен работать
3. **Загрузка данных** - данные должны загружаться из Firestore
4. **Загрузка изображений** - аватары и фотографии должны загружаться
5. **Создание заказов** - заказы должны создаваться и сохраняться
6. **Оплата** - оплата должна работать корректно
7. **Производительность** - приложение должно работать плавно

## ⚠️ Важные замечания

1. **Keystore**: Без keystore нельзя обновлять приложение в Play Store
2. **Версионирование**: Каждый новый релиз должен иметь больший versionCode
3. **Тестирование**: Всегда тестируйте релизную сборку перед публикацией
4. **Размер**: Следите за размером приложения (Play Store ограничение 150 МБ)

## 📤 После сборки

1. Загрузите AAB в [Google Play Console](https://play.google.com/console)
2. Заполните информацию о приложении
3. Добавьте скриншоты
4. Отправьте на проверку

Подробнее см. [PLAY_STORE_PUBLICATION.md](./PLAY_STORE_PUBLICATION.md)

