# 📊 Настройка Firebase Analytics

## ✅ Что уже настроено

1. **Зависимость**: `firebase_analytics: ^11.3.3` добавлен в `pubspec.yaml`
2. **Сервис**: `AnalyticsService` создан с методами для отслеживания событий
3. **Интеграция**: Analytics Observer добавлен в `main.dart` для автоматического отслеживания экранов
4. **События**: Добавлено логирование входа пользователя

## 📊 Отслеживаемые события

### Автоматические события
- **Просмотр экранов** - автоматически через `AnalyticsObserver`
- **Вход пользователя** - при успешной авторизации

### События, которые нужно добавить вручную

#### 1. Создание заказа
```dart
await AnalyticsService.logOrderCreated(
  orderId: order.id,
  specialistId: order.specialistId,
  amount: order.totalPrice,
  category: order.category,
);
```

#### 2. Оплата
```dart
// Начало оплаты
await AnalyticsService.logPaymentStarted(
  orderId: orderId,
  paymentMethod: 'click', // или 'payme', 'cash'
  amount: amount,
);

// Завершение оплаты
await AnalyticsService.logPaymentCompleted(
  orderId: orderId,
  paymentMethod: 'click',
  amount: amount,
);
```

#### 3. Просмотр специалиста
```dart
await AnalyticsService.logSpecialistViewed(
  specialistId: specialistId,
  category: specialist.category,
);
```

#### 4. Поиск
```dart
await AnalyticsService.logSearch(
  searchTerm: searchTerm,
  category: selectedCategory,
  resultCount: results.length,
);
```

#### 5. Отправка сообщения
```dart
await AnalyticsService.logMessageSent(
  chatId: chatId,
  recipientId: recipientId,
);
```

#### 6. Создание отзыва
```dart
await AnalyticsService.logReviewCreated(
  reviewId: reviewId,
  specialistId: specialistId,
  rating: rating,
);
```

## 🔍 Просмотр аналитики

### Firebase Console
1. Откройте: https://console.firebase.google.com/project/odo-uz-1f4d9/analytics
2. Перейдите в раздел "Events" для просмотра событий
3. Используйте "Dashboard" для обзора метрик

### Основные метрики
- **Active Users** - активные пользователи
- **Events** - количество событий
- **Conversions** - конверсии (создание заказа, оплата)
- **User Properties** - свойства пользователей (тип, категория)

## 📈 Полезные отчеты

### 1. Воронка конверсии
- Просмотр специалиста → Создание заказа → Оплата → Завершение

### 2. Популярные категории
- Какие категории специалистов наиболее популярны
- Какие категории приводят к заказам

### 3. Методы оплаты
- Какой метод оплаты предпочитают пользователи
- Конверсия по методам оплаты

### 4. Поиск
- Популярные поисковые запросы
- Эффективность поиска (количество результатов)

## 🧪 Тестирование

### Включить режим отладки
В Firebase Console → Analytics → DebugView можно видеть события в реальном времени.

### Проверка событий
1. Выполните действие в приложении (например, создайте заказ)
2. Проверьте DebugView в Firebase Console
3. Событие должно появиться в течение нескольких секунд

## 💡 Рекомендации

1. **Не логируйте персональные данные** - используйте только ID и агрегированные данные
2. **Используйте осмысленные имена событий** - это упростит анализ
3. **Добавляйте параметры** - они помогут в анализе
4. **Тестируйте события** - убедитесь, что они логируются правильно

## 📚 Дополнительные ресурсы

- [Firebase Analytics Documentation](https://firebase.google.com/docs/analytics)
- [Flutter Firebase Analytics](https://firebase.flutter.dev/docs/analytics/overview)
- [Best Practices for Analytics](https://firebase.google.com/docs/analytics/best-practices)

