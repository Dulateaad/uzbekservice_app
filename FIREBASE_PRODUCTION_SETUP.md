# 🔥 Настройка Firebase для продакшена

## 📋 Текущая ситуация
- Firestore уже настроен на продакшене
- Нужно настроить правила для разработки
- Приложение получает ошибки permissions

## 🔧 Решение

### 1. Проверьте текущие правила Firestore

Откройте [Firebase Console](https://console.firebase.google.com) → проект `odo-uz-app` → Firestore Database → Rules

### 2. Временные правила для разработки

Если текущие правила слишком строгие, замените их на:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Правила для разработки - разрешают все операции
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

### 3. Правила для продакшена (рекомендуемые)

После тестирования замените на более безопасные:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Пользователи могут читать и писать свои данные
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Специалисты могут читать все, писать только свои данные
    match /specialists/{specialistId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == specialistId;
    }
    
    // Заказы - только для участников
    match /orders/{orderId} {
      allow read, write: if request.auth != null && 
        (resource.data.clientId == request.auth.uid || 
         resource.data.specialistId == request.auth.uid);
    }
    
    // Отзывы - только для участников
    match /reviews/{reviewId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && 
        (resource.data.clientId == request.auth.uid);
    }
  }
}
```

### 4. Альтернативное решение - тестовые данные

Если не хотите менять правила, можно:

1. **Создать тестовую коллекцию** с открытыми правами
2. **Использовать анонимную аутентификацию** (уже включена)
3. **Добавить тестовые данные** вручную

### 5. Проверка аутентификации

Убедитесь, что в Firebase Console включены:
- ✅ Phone Authentication
- ✅ Anonymous Authentication (для разработки)

## 🚀 Быстрый тест

После настройки правил:
1. Перезапустите приложение
2. Попробуйте зарегистрироваться
3. Проверьте, что данные сохраняются в Firestore

## 📞 Поддержка

Если проблемы продолжаются, проверьте:
- Правильность API ключей в `firebase_config.dart`
- Настройки проекта в Firebase Console
- Логи в браузере (F12 → Console)
