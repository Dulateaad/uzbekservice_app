# 🔧 Альтернативные методы сборки (обход Sandbox)

## ❌ Проблема:

Sandbox ошибки продолжаются даже после исправления прав доступа. Это системное ограничение macOS 26.1.

## ✅ Альтернативные решения:

### 🥇 Решение 1: Сборка через Flutter напрямую (ОБХОДИТ Xcode Sandbox)

Этот метод использует Flutter CLI, который часто обходит Sandbox проблемы:

```bash
cd ~/uzbekservice_app

# Очистить предыдущие сборки
flutter clean

# Собрать IPA напрямую (для App Store)
flutter build ipa --release

# ИЛИ для Ad Hoc распространения
flutter build ipa --release --export-method ad-hoc
```

**Преимущества:**
- Обходит Xcode Sandbox проблемы
- Создает готовый IPA файл
- Можно загрузить в App Store Connect через Transporter

**После сборки:**
- IPA файл будет в: `build/ios/ipa/`
- Загрузите его в App Store Connect через **Transporter** app

### 🥈 Решение 2: Использовать стандартный DerivedData путь

Попробуйте вернуть DerivedData на стандартный путь:

1. **Xcode → Settings** (`Cmd + ,`)
2. **Locations** → **Derived Data**
3. Выберите **"Default"** (не Custom)
4. Перезапустите Xcode
5. Попробуйте Archive снова

**Стандартный путь:** `~/Library/Developer/Xcode/DerivedData/`

### 🥉 Решение 3: Сборка через xcodebuild в терминале

Попробуйте сборку через командную строку:

```bash
cd ~/uzbekservice_app/ios

# Очистить предыдущие сборки
xcodebuild clean -workspace Runner.xcworkspace -scheme Runner

# Создать Archive
xcodebuild archive \
  -workspace Runner.xcworkspace \
  -scheme Runner \
  -configuration Release \
  -destination 'generic/platform=iOS' \
  -archivePath ~/Desktop/Runner.xcarchive \
  CODE_SIGN_IDENTITY="" \
  CODE_SIGNING_REQUIRED=NO
```

**Примечание:** Для загрузки в App Store нужна кодовая подпись, но для создания Archive можно обойти.

### 🔧 Решение 4: Исправить права на конкретную директорию

Исправьте права на конкретную директорию, которая блокируется:

```bash
sudo chown -R $(whoami) ~/XcodeDerivedData/Runner-apvymelgngogdbbmheixdzwnujrn/
sudo chmod -R u+w ~/XcodeDerivedData/Runner-apvymelgngogdbbmheixdzwnujrn/
```

### 🔄 Решение 5: Перезапустить Mac

Иногда помогает полный перезапуск:

1. Сохраните все работы
2. Полностью перезапустите Mac
3. После перезапуска попробуйте сборку снова

## 💡 Рекомендация:

**Начните с Решения 1** (`flutter build ipa`) - это часто обходит все Sandbox проблемы и создает готовый IPA для загрузки в App Store Connect.

## 📦 Загрузка IPA в App Store Connect:

После создания IPA через `flutter build ipa`:

1. Откройте **Transporter** app (из App Store)
2. Перетащите IPA файл в Transporter
3. Нажмите **Deliver**
4. IPA будет загружен в App Store Connect

**Путь к IPA:** `~/uzbekservice_app/build/ios/ipa/`

## ⚠️ Важно:

- **macOS 26.1** - это очень новая версия, возможно beta
- Beta версии macOS часто имеют проблемы с Sandbox
- `flutter build ipa` часто обходит эти проблемы

## 🔍 Проверка:

После `flutter build ipa` проверьте:

```bash
ls -lh ~/uzbekservice_app/build/ios/ipa/
```

Должен быть файл `.ipa` готовый для загрузки.

