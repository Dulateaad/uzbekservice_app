# 🗺️ Настройка Google Maps API

## ✅ Что уже сделано

1. **📦 Добавлены зависимости** в `pubspec.yaml`:
   - `google_maps_flutter: ^2.5.0`
   - `geolocator: ^10.1.0`
   - `geocoding: ^2.1.1`

2. **🌐 Обновлен `web/index.html`** с Google Maps JavaScript API
3. **🔧 Создан `GoogleMapsService`** для работы с картами
4. **🎨 Создан `GoogleMapsWidget`** для отображения карт
5. **📱 Обновлен экран карт** с интеграцией Google Maps

## 🔑 Настройка Google Maps API

### 1. Получите API ключ

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com)
2. Создайте новый проект или выберите существующий
3. Включите следующие API:
   - **Maps JavaScript API**
   - **Places API**
   - **Geocoding API**
   - **Geolocation API**

4. Создайте API ключ:
   - Перейдите в **APIs & Services** → **Credentials**
   - Нажмите **Create Credentials** → **API Key**
   - Скопируйте созданный ключ

### 2. Обновите API ключ в коде

#### В `web/index.html`:
```html
<!-- Замените YOUR_GOOGLE_MAPS_API_KEY на ваш реальный ключ -->
<script async defer 
  src="https://maps.googleapis.com/maps/api/js?key=ВАШ_API_КЛЮЧ&libraries=places&callback=initMap">
</script>
```

#### В `lib/services/google_maps_service.dart`:
```dart
// В функции getGoogleMapsApiKey() замените на ваш ключ
function getGoogleMapsApiKey() {
  return 'ВАШ_API_КЛЮЧ';
}
```

### 3. Настройте ограничения API ключа (рекомендуется)

1. В Google Cloud Console → **APIs & Services** → **Credentials**
2. Нажмите на ваш API ключ
3. В разделе **Application restrictions**:
   - Выберите **HTTP referrers (web sites)**
   - Добавьте ваши домены:
     - `localhost:*` (для разработки)
     - `yourdomain.com/*` (для продакшена)

4. В разделе **API restrictions**:
   - Выберите **Restrict key**
   - Выберите только нужные API:
     - Maps JavaScript API
     - Places API
     - Geocoding API

## 🚀 Запуск приложения

### Веб-версия (с Google Maps):
```bash
flutter run -d chrome --web-port 8080
```

### Мобильная версия (без Google Maps):
```bash
flutter run -d android
flutter run -d ios
```

## 📱 Функциональность

### ✅ Что работает:

1. **🗺️ Отображение карты** с центром в Ташкенте
2. **📍 Получение текущего местоположения** пользователя
3. **🎯 Маркеры специалистов** на карте
4. **🎨 Кастомные стили** карты в цветах приложения
5. **📱 Адаптивный дизайн** для разных экранов

### 🔧 Возможности:

1. **Поиск мест** через Places API
2. **Геокодирование** адресов
3. **Выбор местоположения** для заказов
4. **Отображение специалистов** на карте
5. **Маршруты** между точками

## 🎨 Стилизация карты

Карта использует кастомные стили в цветах приложения:
- **Основной цвет**: #00E676 (Lime)
- **Вторичный цвет**: #03A9F4 (Blue)
- **Стиль**: Минималистичный, светлый

## 🔒 Безопасность

### Рекомендации:

1. **Ограничьте API ключ** по доменам
2. **Используйте разные ключи** для разработки и продакшена
3. **Не храните ключи** в публичных репозиториях
4. **Регулярно ротируйте** API ключи

### Для продакшена:

1. Создайте отдельный API ключ
2. Ограничьте его только вашими доменами
3. Используйте переменные окружения для хранения ключей

## 🐛 Решение проблем

### Карта не загружается:
1. Проверьте API ключ
2. Убедитесь, что API включены
3. Проверьте ограничения доменов
4. Посмотрите консоль браузера на ошибки

### Геолокация не работает:
1. Убедитесь, что сайт использует HTTPS
2. Проверьте разрешения браузера
3. Проверьте настройки API ключа

### Маркеры не отображаются:
1. Проверьте координаты
2. Убедитесь, что они в пределах видимой области карты
3. Проверьте иконки маркеров

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте [документацию Google Maps](https://developers.google.com/maps/documentation)
2. Посмотрите логи в консоли браузера
3. Проверьте настройки API в Google Cloud Console

---

**🎉 После настройки API ключа Google Maps будет работать в веб-версии приложения!**
