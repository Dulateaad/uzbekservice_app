# 🔧 Исправление путей xcfilelist

## ❌ Проблема:

Xcode ищет файлы по абсолютному пути `/Target Support Files/...` вместо `${PODS_ROOT}/Target Support Files/...`.

Это означает, что переменная `${PODS_ROOT}` не разрешается правильно.

## ✅ Решение:

### Шаг 1: Перезапустите Xcode

1. **Полностью закройте Xcode** (`Cmd + Q`)
2. Откройте проект заново:
   ```bash
   open ~/uzbekservice_app/ios/Runner.xcworkspace
   ```

### Шаг 2: Очистите Build

1. **Product → Clean Build Folder** (`Shift + Cmd + K`)
2. Дождитесь завершения

### Шаг 3: Проверьте настройки проекта

1. Выберите проект **Runner** в навигаторе
2. Выберите target **Runner**
3. Перейдите на вкладку **Build Settings**
4. Найдите **"PODS_ROOT"** в поиске
5. Убедитесь, что значение: `$(SRCROOT)/Pods`

### Шаг 4: Попробуйте сборку

1. **Product → Build** (`Cmd + B`)

## 🔍 Если проблема сохраняется:

Проблема может быть связана с изменением DerivedData пути. Попробуйте:

### Вариант 1: Вернуть DerivedData на Default

1. **Xcode → Settings** (`Cmd + ,`)
2. **Locations** → **Derived Data**
3. Выберите **"Default"** вместо "Custom"
4. Перезапустите Xcode
5. Попробуйте сборку

### Вариант 2: Проверить переменную SRCROOT

В Build Settings проверьте, что **SRCROOT** определен правильно:
- Должно быть: `$(PROJECT_DIR)`

## 💡 Важно:

Переменная `PODS_ROOT` определяется в файле:
```
Pods/Target Support Files/Pods-Runner/Pods-Runner.debug.xcconfig
```

Как: `PODS_ROOT = ${SRCROOT}/Pods`

Если `SRCROOT` не определен, `PODS_ROOT` тоже не будет работать.

