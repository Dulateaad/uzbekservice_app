# 🔧 Альтернативные решения Sandbox проблем

## ❌ Проблема:
Sandbox ошибки продолжаются даже при наличии Full Disk Access у Xcode.

## ✅ Альтернативные решения:

### 🥇 Решение 1: Сборка через Xcode напрямую (ОБХОДИТ Sandbox)

**Проект уже открыт в Xcode!**

1. В Xcode:
   - Выберите **"Any iOS Device"** в верхней панели (НЕ симулятор!)
   - **Product** → **Scheme** → **Edit Scheme...**
   - В левой колонке выберите **"Archive"**
   - **Build Configuration**: выберите **"Release"**
   - Нажмите **Close**

2. Создайте Archive:
   - **Product** → **Archive**
   - Дождитесь завершения (5-15 минут)

3. Загрузите в TestFlight:
   - После завершения откроется **Organizer**
   - Нажмите **Distribute App**
   - Выберите **App Store Connect**
   - Следуйте инструкциям

**Этот метод часто обходит все Sandbox проблемы!**

### 🥈 Решение 2: Перезапустить Mac

Иногда помогает перезапуск системы:

1. Сохраните все работы
2. Полностью перезапустите Mac
3. После перезапуска попробуйте сборку снова

**Почему это работает:** Перезапуск сбрасывает все Sandbox сессии.

### 🥉 Решение 3: Использовать другой путь для сборки

Попробуйте собрать в другой директории:

```bash
cd ~
cp -r uzbekservice_app uzbekservice_app_backup
cd uzbekservice_app
flutter clean
flutter pub get
cd ios && pod install
cd ..
flutter build ios --release
```

### 🔧 Решение 4: Проверить настройки проекта

1. Откройте проект в Xcode
2. Выберите проект **Runner** в навигаторе
3. Выберите target **Runner**
4. Перейдите на вкладку **Signing & Capabilities**
5. Проверьте, что:
   - **Automatically manage signing** включен
   - Выбран правильный Team
   - Bundle Identifier правильный

### 🔍 Решение 5: Проверить версию macOS

Ваша версия macOS: **26.1** (очень новая, возможно beta)

Некоторые beta версии macOS имеют более строгие Sandbox правила.

**Попробуйте:**
- Обновить macOS до стабильной версии
- Или использовать другой Mac для сборки

### 🛠️ Решение 6: Использовать Flutter build с другими параметрами

```bash
cd ~/uzbekservice_app
flutter build ios --release --no-codesign
```

Или попробуйте сборку для симулятора (для тестирования):

```bash
flutter build ios --simulator --release
```

### 📱 Решение 7: Сборка через командную строку Xcode

```bash
cd ~/uzbekservice_app/ios
xcodebuild -workspace Runner.xcworkspace \
  -scheme Runner \
  -configuration Release \
  -destination 'generic/platform=iOS' \
  archive \
  -archivePath build/Runner.xcarchive
```

## 💡 Рекомендация:

**Начните с Решения 1** (сборка через Xcode напрямую) - это часто обходит все Sandbox проблемы и позволяет сразу создать Archive для TestFlight.

Если не помогает, попробуйте **Решение 2** (перезапуск Mac).

## ⚠️ Важно:

- Убедитесь, что Xcode действительно в Full Disk Access
- После добавления в Full Disk Access **обязательно перезапустите Xcode**
- Проверьте версию macOS - beta версии могут иметь проблемы

## 🔍 Диагностика:

Проверьте логи Sandbox:

1. Откройте **Console.app** (Applications → Utilities)
2. В поиске введите: `sandbox`
3. Посмотрите логи - должны быть разрешения для Xcode

---

**Скрипт для быстрого открытия проекта:**
```bash
./build_via_xcode.sh
```

