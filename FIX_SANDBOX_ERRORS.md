# 🔧 Исправление Sandbox ошибок

## ❌ Ошибка:
```
Build Failed
4 Sandbox Errors:
- file-read-data errors
- file-write-create errors
```

## ✅ Причина:
Ошибки Sandbox возникают когда Xcode не может получить доступ к файлам проекта из-за:
- Поврежденного Derived Data
- Проблем с правами доступа
- Кэша Xcode

## 🔧 Решение:

### Шаг 1: Удалите Derived Data

1. В Xcode: **Preferences** (`Cmd + ,`)
2. Вкладка **Locations**
3. Найдите **Derived Data**
4. Нажмите на **стрелку** рядом с путем
5. В Finder откроется папка Derived Data
6. **Удалите папку** для вашего проекта (или всю папку Derived Data)

### Шаг 2: Очистите проект

```bash
cd ~/uzbekservice_app
flutter clean
flutter pub get
```

### Шаг 3: В Xcode

1. **Product** → **Clean Build Folder** (`Shift + Cmd + K`)

2. **Закройте Xcode полностью** (`Cmd + Q`)

3. **Откройте снова:**
   ```bash
   open ~/uzbekservice_app/ios/Runner.xcworkspace
   ```

4. **Product** → **Build** (`Cmd + B`)

### Шаг 4: Если проблема сохраняется

#### Вариант 1: Полная очистка

```bash
cd ~/uzbekservice_app
flutter clean
rm -rf ios/Pods ios/Podfile.lock ios/.symlinks
rm -rf ~/Library/Developer/Xcode/DerivedData/*
flutter pub get
cd ios
pod install
cd ..
```

#### Вариант 2: Проверьте права доступа

```bash
# Проверьте права на папку проекта
ls -la ~/uzbekservice_app

# Если нужно, исправьте права
chmod -R 755 ~/uzbekservice_app
```

#### Вариант 3: Перезапустите Mac

Иногда помогает полный перезапуск системы.

## 🔍 Альтернативное решение:

### Через терминал (удаление Derived Data):

```bash
# Удалите все Derived Data
rm -rf ~/Library/Developer/Xcode/DerivedData/*

# Или только для вашего проекта
rm -rf ~/Library/Developer/Xcode/DerivedData/Runner-*
```

## ✅ После исправления:

- ✅ Sandbox ошибки должны исчезнуть
- ✅ Статус должен измениться на "Build Succeeded"
- ✅ Можно создавать Archive

## 📱 После успешной сборки:

1. **Product** → **Archive**
2. **Organizer** → **Distribute App**
3. **App Store Connect** → **Upload**
4. Загрузка в TestFlight

## 💡 Профилактика:

- Регулярно очищайте Derived Data
- Используйте `flutter clean` перед важными сборками
- Закрывайте Xcode полностью перед пересборкой

