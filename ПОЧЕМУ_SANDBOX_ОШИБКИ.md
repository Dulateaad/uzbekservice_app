# 🔍 Почему возникают Sandbox ошибки?

## 🎯 Что такое Sandbox?

**Sandbox (песочница)** - это система безопасности macOS, которая ограничивает доступ приложений к файлам и ресурсам системы.

---

## ❌ Почему возникают ошибки:

### 1. **macOS 26.1 (Sequoia) - Усиленная защита**

В новых версиях macOS Apple ужесточила sandbox политику:
- Приложения не могут читать/писать файлы без явного разрешения
- Xcode требует специальных прав для работы с проектами
- Даже системные инструменты ограничены

### 2. **Xcode не имеет Full Disk Access**

**Самая частая причина:**
- Xcode не может читать файлы проекта
- Не может писать в DerivedData
- Не может создавать build артефакты

**Ошибки:**
```
Sandbox: rsync deny file-read-data
Sandbox: dartvm deny file-write-create
```

### 3. **Проблемы с правами на файлы**

- Файлы проекта принадлежат другому пользователю
- Нет прав на запись в папки сборки
- DerivedData заблокирован

### 4. **Конфликты с другими процессами**

- Xcode открыт в другом окне
- Другие процессы используют файлы
- Кэш заблокирован

---

## ✅ Решения:

### Решение 1: Full Disk Access (ГЛАВНОЕ!)

**Это самое важное исправление:**

1. **Откройте System Settings:**
   - Нажмите на **🍎 Apple** → **System Settings**

2. **Перейдите в Privacy & Security:**
   - Найдите **Privacy & Security** в списке

3. **Найдите Full Disk Access:**
   - Прокрутите вниз до **Full Disk Access**

4. **Добавьте Xcode:**
   - Нажмите **+** (плюс)
   - Найдите **Xcode** в `/Applications`
   - Выберите и нажмите **Open**
   - **Включите** переключатель (должен стать синим/зеленым)

5. **Добавьте Terminal (или iTerm):**
   - Также добавьте **Terminal** или **iTerm**
   - Это нужно для команд из терминала

6. **ПЕРЕЗАПУСТИТЕ MAC:**
   - Это критически важно!
   - После перезапуска права вступят в силу

### Решение 2: Исправить права на файлы

```bash
cd ~/uzbekservice_app

# Исправить права на проект
sudo chown -R $(whoami) .

# Исправить права на DerivedData
sudo chown -R $(whoami) ~/Library/Developer/Xcode/DerivedData/

# Дать права на запись
sudo chmod -R u+w ~/Library/Developer/Xcode/DerivedData/
```

**⚠️ Требует пароль администратора**

### Решение 3: Закрыть все процессы Xcode

```bash
# Закрыть все процессы Xcode
killall Xcode 2>/dev/null || true

# Подождать 5 секунд
sleep 5

# Открыть заново
open ~/uzbekservice_app/ios/Runner.xcworkspace
```

### Решение 4: Очистить кэш

```bash
cd ~/uzbekservice_app

# Очистить Flutter кэш
flutter clean

# Очистить Xcode DerivedData
rm -rf ~/Library/Developer/Xcode/DerivedData/*

# Очистить CocoaPods кэш
cd ios
rm -rf Pods Podfile.lock
pod cache clean --all
```

---

## 🔍 Как проверить, что исправлено:

### Проверка 1: Full Disk Access

1. System Settings → Privacy & Security → Full Disk Access
2. Убедитесь, что **Xcode** включен (синий переключатель)
3. Убедитесь, что **Terminal** включен

### Проверка 2: Права на файлы

```bash
# Проверить владельца проекта
ls -ld ~/uzbekservice_app

# Должно быть: dulatea (ваше имя пользователя)
```

### Проверка 3: Попробовать сборку

```bash
cd ~/uzbekservice_app
flutter build ios --release --no-codesign
```

Если ошибок нет - значит исправлено!

---

## 📊 Типичные ошибки и их причины:

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Sandbox: rsync deny file-read-data` | Нет Full Disk Access | Включить в настройках |
| `Sandbox: dartvm deny file-write-create` | Нет прав на запись | Исправить права на файлы |
| `Sandbox: rsync deny file-read-data /DerivedData` | DerivedData заблокирован | Очистить и исправить права |
| `Unable to open Release.xcconfig` | Файлы не найдены | flutter precache --ios |

---

## 🎯 Пошаговый план исправления:

### Шаг 1: Full Disk Access (ОБЯЗАТЕЛЬНО!)

1. System Settings → Privacy & Security → Full Disk Access
2. Добавить Xcode и Terminal
3. **Перезапустить Mac**

### Шаг 2: После перезапуска

```bash
cd ~/uzbekservice_app

# Исправить права (требует пароль)
sudo chown -R $(whoami) ~/Library/Developer/Xcode/DerivedData/
sudo chmod -R u+w ~/Library/Developer/Xcode/DerivedData/
```

### Шаг 3: Очистить и пересобрать

```bash
cd ~/uzbekservice_app
flutter clean
flutter pub get
flutter build ios --release --no-codesign
```

---

## ⚠️ Почему это важно:

**Без Full Disk Access:**
- ❌ Xcode не может читать файлы проекта
- ❌ Не может писать в DerivedData
- ❌ Не может создавать build артефакты
- ❌ Сборка всегда будет падать с sandbox ошибками

**С Full Disk Access:**
- ✅ Xcode может работать с файлами
- ✅ Может создавать build артефакты
- ✅ Сборка проходит успешно

---

## 🔗 Полезные ссылки:

- **System Settings:** Apple → System Settings → Privacy & Security
- **Apple Developer:** https://developer.apple.com

---

## 💡 Важно помнить:

1. **Full Disk Access** - это не опционально, это обязательно для Xcode
2. **Перезапуск Mac** необходим после включения Full Disk Access
3. **Права на файлы** нужно исправлять с sudo (требует пароль)

---

**Начните с Full Disk Access - это решит 90% проблем!**

