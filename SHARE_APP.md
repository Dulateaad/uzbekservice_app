# Как сделать приложение доступным для других

## Вариант 1: Локальная сеть (быстро, для тестирования)

### Запуск с доступом по сети:

```bash
flutter run -d chrome --web-hostname 0.0.0.0 --web-port=8112
```

### Доступ:
- **Ваш IP:** `192.168.0.113` или `192.168.0.119`
- **URL для других:** `http://192.168.0.113:8112` или `http://192.168.0.119:8112`

### Важно:
- ✅ Все устройства в той же Wi-Fi сети смогут открыть приложение
- ⚠️ Работает только пока приложение запущено
- ⚠️ Не работает через интернет (только локальная сеть)

---

## Вариант 2: Firebase Hosting (рекомендуется для production)

### Шаг 1: Установка Firebase CLI

```bash
npm install -g firebase-tools
firebase login
```

### Шаг 2: Инициализация Firebase Hosting

```bash
cd /Users/dulat/uzbekservice_app
firebase init hosting
```

Выберите:
- **Existing project:** `studio-3898272712-a12a4`
- **Public directory:** `build/web`
- **Single-page app:** `Yes`
- **Automatic builds:** `No`

### Шаг 3: Сборка и деплой

```bash
# Сборка для production
flutter build web --release

# Деплой на Firebase Hosting
firebase deploy --only hosting
```

### Доступ:
После деплоя приложение будет доступно по адресу:
`https://studio-3898272712-a12a4.web.app` или `https://studio-3898272712-a12a4.firebaseapp.com`

---

## Вариант 3: ngrok (для временного доступа через интернет)

### Установка ngrok:

```bash
# macOS
brew install ngrok

# Или скачайте с https://ngrok.com/download
```

### Использование:

1. Запустите приложение:
```bash
flutter run -d chrome --web-port=8112
```

2. В другом терминале запустите ngrok:
```bash
ngrok http 8112
```

3. Скопируйте URL из ngrok (например: `https://abc123.ngrok.io`)

### Доступ:
- ✅ Работает через интернет
- ✅ Можно дать ссылку любому
- ⚠️ Бесплатный план имеет ограничения (время работы, количество запросов)

---

## Вариант 4: Render/Vercel (для постоянного доступа)

### Render:

1. Создайте `render.yaml`:
```yaml
services:
  - type: web
    name: odo-uz-app
    env: static
    buildCommand: flutter build web --release
    staticPublishPath: build/web
```

2. Подключите GitHub репозиторий
3. Render автоматически задеплоит приложение

### Vercel:

1. Установите Vercel CLI:
```bash
npm install -g vercel
```

2. Деплой:
```bash
flutter build web --release
cd build/web
vercel --prod
```

---

## Быстрый старт (локальная сеть)

Создайте скрипт `share_app.sh`:

```bash
#!/bin/bash
echo "🌐 Запуск приложения с доступом по сети..."
echo "📱 Ваш IP: $(ifconfig | grep 'inet ' | grep -v 127.0.0.1 | head -1 | awk '{print $2}')"
echo "🔗 URL для других: http://$(ifconfig | grep 'inet ' | grep -v 127.0.0.1 | head -1 | awk '{print $2}'):8112"
echo ""
flutter run -d chrome --web-hostname 0.0.0.0 --web-port=8112
```

Запуск:
```bash
chmod +x share_app.sh
./share_app.sh
```

---

## Рекомендации

### Для быстрого тестирования с командой:
→ **Вариант 1** (локальная сеть)

### Для демонстрации клиентам:
→ **Вариант 3** (ngrok) или **Вариант 2** (Firebase Hosting)

### Для production:
→ **Вариант 2** (Firebase Hosting) или **Вариант 4** (Render/Vercel)

---

## Проверка доступа

После запуска с `--web-hostname 0.0.0.0`:
1. Откройте на телефоне/планшете: `http://192.168.0.113:8112`
2. Убедитесь, что устройство подключено к той же Wi-Fi сети
3. Если не открывается, проверьте файрвол на Mac

