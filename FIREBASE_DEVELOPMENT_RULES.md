# 🔥 Правила Firestore для разработки

## 📋 Рекомендуемые правила для тестирования

Используйте эти правила для разработки и тестирования:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Временные правила для разработки - разрешают все операции аутентифицированным пользователям
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

## 🚀 Как применить

1. Откройте [Firebase Console](https://console.firebase.google.com)
2. Выберите проект `odo-uz-app`
3. Перейдите в **Firestore Database > Rules**
4. Замените правила на код выше
5. Нажмите **"Publish"**

## ✅ Что это даст

- ✅ Аутентифицированные пользователи могут читать и писать все данные
- ✅ Приложение будет работать корректно
- ✅ Можно тестировать все функции
- ✅ Данные будут сохраняться в Firestore

## 🔒 Безопасность

⚠️ **ВНИМАНИЕ**: Эти правила дают полный доступ к базе данных. Используйте только для разработки!

Для продакшена используйте более строгие правила из `FIREBASE_PRODUCTION_RULES_FIXED.md`.
