# 🧪 Тестирование Push-уведомлений

## Шаг 1: Запустить приложение и получить токен

1. **Запустите приложение**:
   ```bash
   cd ~/uzbekservice_app
   flutter run
   ```

2. **Войдите в аккаунт** (через SMS или OneID)

3. **Проверьте логи** - вы должны увидеть:
   ```
   📱 Инициализация push-уведомлений...
   📱 Разрешения на уведомления: AuthorizationStatus.authorized
   ✅ Разрешение на уведомления получено
   📱 FCM Token получен: [первые 20 символов токена]...
   ✅ Токен сохранен в профиле пользователя
   ```

4. **Скопируйте полный токен** из логов или используйте скрипт для получения токена из Firestore

## Шаг 2: Получить токен устройства

### Вариант A: Из логов приложения
- Найдите в консоли строку: `📱 FCM Token получен: ...`
- Скопируйте полный токен (длинная строка)

### Вариант B: Из Firestore
1. Откройте Firebase Console: https://console.firebase.google.com/project/odo-uz-1f4d9
2. Перейдите в Firestore Database
3. Найдите документ пользователя в коллекции `users`
4. Скопируйте токен из поля `deviceTokens` (массив, первый элемент)

### Вариант C: Использовать скрипт
```bash
cd ~/uzbekservice_app
./get_fcm_token.sh
```

## Шаг 3: Отправить тестовое уведомление через Firebase Console

1. **Откройте Firebase Console**:
   https://console.firebase.google.com/project/odo-uz-1f4d9/notification

2. **Нажмите "Send your first message"** или "New notification"

3. **Заполните форму**:
   - **Notification title**: "Тестовое уведомление"
   - **Notification text**: "Это тестовое сообщение для проверки push-уведомлений"
   - **Notification image** (опционально): можно добавить изображение

4. **Выберите целевую аудиторию**:
   - Выберите "Send test message"
   - Вставьте FCM токен устройства
   - Нажмите "Test"

5. **Или отправьте всем пользователям**:
   - Выберите "User segment"
   - Можно выбрать по условиям или отправить всем

6. **Добавьте дополнительные данные** (опционально):
   - В разделе "Additional options" → "Custom data"
   - Добавьте:
     ```
     type: order
     orderId: test123
     ```

7. **Нажмите "Review"** → "Publish"

## Шаг 4: Проверить получение уведомления

### Если приложение открыто (Foreground):
- Уведомление должно появиться в логах:
  ```
  📨 Получено уведомление (foreground):
     Заголовок: Тестовое уведомление
     Текст: Это тестовое сообщение...
     Данные: {type: order, orderId: test123}
  ```

### Если приложение закрыто (Background):
- Уведомление должно появиться в системной панели уведомлений
- При клике на уведомление приложение должно открыться
- В логах должно появиться:
  ```
  📨 Уведомление открыло приложение:
     Заголовок: Тестовое уведомление
     Данные: {type: order, orderId: test123}
  ```

## Шаг 5: Отправить уведомление через REST API (для автоматизации)

### Получить Access Token:
```bash
# Установите gcloud CLI (если еще не установлен)
# Затем выполните:
gcloud auth application-default login
gcloud config set project odo-uz-1f4d9

# Получите access token
gcloud auth print-access-token
```

### Отправить уведомление:
```bash
curl -X POST https://fcm.googleapis.com/v1/projects/odo-uz-1f4d9/messages:send \
  -H "Authorization: Bearer YOUR_ACCESS_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "message": {
      "token": "YOUR_FCM_TOKEN",
      "notification": {
        "title": "Тестовое уведомление",
        "body": "Это тестовое сообщение"
      },
      "data": {
        "type": "order",
        "orderId": "test123"
      }
    }
  }'
```

## 🐛 Отладка проблем

### Проблема: Токен не получен
- Проверьте разрешения на уведомления
- Убедитесь, что Firebase инициализирован
- Проверьте логи на ошибки

### Проблема: Уведомление не приходит
- Проверьте, что токен правильный
- Убедитесь, что уведомление отправлено на правильный токен
- Проверьте настройки уведомлений в системе (Android/iOS)
- Для Android: проверьте, что Google Play Services обновлены

### Проблема: Уведомление приходит, но не обрабатывается
- Проверьте логи приложения
- Убедитесь, что обработчики настроены правильно
- Проверьте формат данных в уведомлении

## 📱 Типы тестовых уведомлений

### 1. Уведомление о новом заказе:
```json
{
  "notification": {
    "title": "Новый заказ!",
    "body": "У вас новый заказ от Иван Иванов"
  },
  "data": {
    "type": "order",
    "orderId": "order123"
  }
}
```

### 2. Уведомление о новом сообщении:
```json
{
  "notification": {
    "title": "Новое сообщение",
    "body": "Иван Иванов: Привет!"
  },
  "data": {
    "type": "chat",
    "chatId": "chat123"
  }
}
```

### 3. Уведомление о специалисте:
```json
{
  "notification": {
    "title": "Новый отзыв",
    "body": "Вам оставили отзыв!"
  },
  "data": {
    "type": "specialist",
    "specialistId": "specialist123",
    "action": "new_review"
  }
}
```

## ✅ Чеклист тестирования

- [ ] Приложение запущено и пользователь авторизован
- [ ] Токен получен и сохранен в Firestore
- [ ] Разрешения на уведомления получены
- [ ] Тестовое уведомление отправлено через Firebase Console
- [ ] Уведомление получено в foreground
- [ ] Уведомление получено в background
- [ ] При клике на уведомление приложение открывается
- [ ] Данные из уведомления обрабатываются правильно

